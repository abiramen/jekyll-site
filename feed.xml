<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2025-12-13T22:29:31+00:00</updated><id>/feed.xml</id><title type="html">the ramen space</title><subtitle>The personal site of @abiramen.</subtitle><entry><title type="html">CompClub CTF 2022</title><link href="/compclub-2022" rel="alternate" type="text/html" title="CompClub CTF 2022" /><published>2023-01-13T00:00:00+00:00</published><updated>2023-01-13T00:00:00+00:00</updated><id>/compclub-2022</id><content type="html" xml:base="/compclub-2022"><![CDATA[<p>After running CompClub’s Intro to Security workshop for some eager high schoolers <a href="/compclub-2021" target="_blank">last year</a>, I just had to come back to redevelop the workshop and present again.</p>

<h1 id="behind-the-scenes">Behind the Scenes</h1>

<p>Setting up infrastructure was much easier this year - being CSESoc’s Technical Director meant I had access to a server to host a CTF. The only other challenge that required hosting was lightweight enough for me to run on my spare VPS.</p>

<p>One thing that was a bit challenging was deciding which challenges I wanted to use for this CTF, as opposed to stockpiling them for the <a href="https://secso.cc" target="_blank">SecSoc</a> O-Week CTF happening in a month (stay tuned!).</p>

<p>Between <a href="https://github.com/felixc90" target="_blank">Felix</a>, <a href="https://github.com/paulatennent">Paula</a> and I, we managed to write up 23 challenges, much more than last year’s 14. Despite that, one team managed to solve all the challenges with an hour remaining, forcing me to release two <strong>more</strong> challenges which I was planning on saving for the aforementioned SecSoc CTF for a grand total of 25.</p>

<p>Huge shoutout to Felix and Paula again for their help with writing up challenges this year!</p>

<h1 id="challenges-and-solutions">Challenges and solutions</h1>
<p>Here are all the challenges! The solutions are hidden until you click on them, so that you can try challenges without spoilers.</p>

<h2 id="crypto">Crypto</h2>
<h3 id="substitute-40-points">substitute (40 points)</h3>
<p>Use this link to help solve a substitution cipher!</p>

<p><a href="https://nsa-crypto.xurt.is/?salt=5244e8b7331750d0&amp;message=L+VJLYZ+VJNTN+LW+YM+WQPWVLVQVN+KMT+EQDGLVC+AJNY+LV+IMHNW+VM+IJMMWLYX+D+XMMO+ILBJNT&amp;hash=c23137bf518741112af3515b4f689eba2b178b47eec468801fed5e8f02e5b2c9">link</a></p>

<p><strong>Format</strong>: Enter the sentence you find, <strong>without</strong> wrapping it in <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>. For example, if you found out that the sentence is ‘never gonna give you up’, then that would be the flag!</p>

<details>

  <h5 id="author">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough">Walkthrough</h5>
  <p>This challenge just required classic substitution cipher cracking techniques, as well as a bit of trial and error.</p>

  <h5 id="flag">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">i think there is no substitute for quality when it comes to choosing a good cipher</code></p>
</details>

<h3 id="witchs-secret-50-points">Witch’s Secret (50 points)</h3>
<p>It’s a simple spell but quite unbreakable. Paula the witch has a confidential message for her coven but wishing for it to be unreadable to man, she enchants it with a special HEX.</p>

<p>Her message may contain plans to destroy humankind so it is important to unenchant this message. It is up to you to decrypt her message and determine what actions we need to take!</p>

<p>Message:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>43 4f 4d 50 43 4c 55 42 7b 77 68 45 52 45 5f 64 30 5f 21 5f 62 75 59 5f 40 5f 6e 33 77 5f 62 52 30 30 6d 53 74 21 63 6b 3f 7d
</code></pre></div></div>

<details>

  <h5 id="author-1">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-1">Walkthrough</h5>
  <p>Reverse the hexadecimal message into their ASCII representation using online tools.</p>

  <h5 id="flag-1">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{whERE_d0_!_buY_@_n3w_bR00mSt!ck?}</code></p>
</details>

<h3 id="musical-sheets-70-points">Musical Sheets (70 points)</h3>

<p>Abiram is getting sick of listening to music in general. He’s invented a new way to create music using spreadsheets which involves two sounds. One is a simple tap while the other is a hold. As monotonous as it is, he’s found that it’s a pretty cool way to hide messages. He calls it ‘Morszart’ - a play on ‘Morse’.</p>

<p>He has hid a secret message in a translated of his favourite Coldplay song ‘Viva La Vida’ into Morszart but it accidentally ripped into three pieces. Can you find the secret message?</p>

<p><strong>Format</strong>: Wrap your answer with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>.</p>

<p><a href="https://docs.google.com/spreadsheets/d/1zQzlWJo1IMTpDdKT0dV88lgCGM971NCSBrQemcwA0B4/edit?usp=sharing">link</a></p>

<details>

  <h5 id="author-2">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-2">Walkthrough</h5>
  <ol>
    <li>Observe that where each sheet never has cells filled in the same position. Combine all sheets in a single sheet.</li>
    <li>Translate the message using morse code.</li>
  </ol>

  <h5 id="flag-2">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{sH33t5.!s.k3wl}</code></p>
</details>

<h3 id="outside-person-80-points">Outside Person (80 points)</h3>

<p>Paula has always been an outside person. She loves the beaches, city, the bush but her favourite place has always been the parks. Sometimes she tells us about a park but instead of saying the actual name, she gives us three words that sound random and expects us to know where it is.</p>

<p>She also says there’s a website that can convert locations to a few words but we’ve never seen it.</p>

<p>We have recently lost her and we know she’s at a park but we don’t know which one. All we’ve found is this piece of text on her laptop:</p>

<p><code class="language-plaintext highlighter-rouge">ixxmit.lyebyd.jywgle</code></p>

<p>but this gibberish doesn’t look like the few words she normally gives us.</p>

<p>Can you help us find her? The flag is the park name, with spaces replaced with underscores, and wrapped in <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code> e.g. <code class="language-plaintext highlighter-rouge">COMPCLUB{central_park}</code>.</p>

<details>

  <h5 id="author-3">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-3">Walkthrough</h5>
  <ol>
    <li>Use multiple caesar ciphers to reveal the words appeal.violin.laying</li>
    <li>Get location from ‘what 3 words’ (search up ‘convert location to 
few words’)</li>
  </ol>

  <h5 id="flag-3">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{tumbalong_park}</code></p>
</details>

<h3 id="fave-song-90-points">fave song (90 points)</h3>

<p>now’s your chance to find out about my favourite song!</p>

<p>i even have some top-secret extra info about the song for you to find out!</p>

<p><strong>Format</strong>: Enter the info you find, <strong>without</strong> wrapping it in <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>. For example, if you found out that the secret is ‘thesongwasreleasedin1066’, then that would be the flag!</p>

<h4 id="files">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/crypto/fave-song/_ctfd/files/fave-song.txt">fave-song.txt</a></li>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/crypto/fave-song/_ctfd/files/more-info.txt">more-info.txt</a></li>
</ul>

<details>

  <h5 id="author-4">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-4">Walkthrough</h5>

  <p>Another year, another book cipher!</p>

  <p>We can see the numbers occur in pairs, <code class="language-plaintext highlighter-rouge">(a, b)</code>. The <code class="language-plaintext highlighter-rouge">a</code> values can get pretty big, while <code class="language-plaintext highlighter-rouge">b</code> is always relatively small (at most, 6). This suggests that the second number could be some way of indexing a character within a word. Maybe the first number could be the index of the word we’re using?</p>

  <p>We can also see a couple of 0s occurring in the index, for example, the a pair is <code class="language-plaintext highlighter-rouge">(0, 0)</code>. This suggests that both words and characters are zero-indexed, just like lists and arrays in most programming languages start counting from 0.</p>

  <p>This is what is known as a book cipher. You could either try manually calculating each of the letters, writing a script to do it, or using a website <a href="https://www.boxentriq.com/code-breaking/book-cipher">like this one</a> to decode it. You’ll need to convert all the index pairs to match the format on that site. Make sure you select the correct options (word number, character number, none), and set the numbering start to 0. Once you do this, you should get your flag.</p>

  <h5 id="flag-4">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">thelinkisdQw4w9WgXcQ</code></p>
</details>

<h2 id="forensics">Forensics</h2>

<h3 id="smoothie-20-points">smoothie (20 points)</h3>

<p>Put these ingredients through the SHA256 blender for a delicious beverage and tell me what you get 😋</p>

<p><strong>Format</strong>: Type your answer in as-is. You <strong>should not</strong> wrap your answer with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>.</p>

<h4 id="files-1">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/smoothie/_ctfd/files/smoothie.jpg">smoothie.jpg</a></li>
</ul>

<details>

  <h5 id="author-5">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-5">Walkthrough</h5>

  <p>There’s a few ways you could do this. If you had access to a Linux or macOS command line, you could run <code class="language-plaintext highlighter-rouge">sha256sum smoothie.jpg</code>, and there’s also <code class="language-plaintext highlighter-rouge">Get-FileHash</code> for Windows Powershell.</p>

  <p>There are also online tools to get you the hashes of files!</p>

  <h5 id="flag-5">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">e2338ea881b3732b5965283c310ddb2db1a632ac27d487dce0bf45b29bfdb034</code></p>
</details>

<h3 id="smoothie-20-points-1">smoothie (20 points)</h3>

<p>Put these ingredients through the SHA256 blender for a delicious beverage and tell me what you get 😋</p>

<p><strong>Format</strong>: Type your answer in as-is. You <strong>should not</strong> wrap your answer with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>.</p>

<h4 id="files-2">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/smoothie/_ctfd/files/smoothie.jpg">smoothie.jpg</a></li>
</ul>

<details>

  <h5 id="author-6">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-6">Walkthrough</h5>

  <p>There’s a few ways you could do this. If you had access to a Linux or macOS command line, you could run <code class="language-plaintext highlighter-rouge">sha256sum smoothie.jpg</code>, and there’s also <code class="language-plaintext highlighter-rouge">Get-FileHash</code> for Windows Powershell.</p>

  <p>There are also online tools to get you the hashes of files!</p>

  <h5 id="flag-6">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">e2338ea881b3732b5965283c310ddb2db1a632ac27d487dce0bf45b29bfdb034</code></p>
</details>

<h3 id="classic-file-extension-challenge-30-points">classic file extension challenge (30 points)</h3>

<p>What file extension would we expect this file to have?</p>

<p><strong>Format</strong>: Wrap your answer with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>.
For example, if this was a PNG file, we might expect the file to be called file.png, and thus the answer would be <code class="language-plaintext highlighter-rouge">COMPCLUB{png}</code>.</p>

<h4 id="files-3">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/classic-file-extension-challenge/_ctfd/files/file">file</a></li>
</ul>

<details>

  <h5 id="author-7">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-7">Walkthrough</h5>

  <p>There’s a few ways you could check what type of file this is meant to be:</p>

  <ul>
    <li>if you have access to a Linux or macOS command line, you could run the <code class="language-plaintext highlighter-rouge">file</code> command, so in this case, <code class="language-plaintext highlighter-rouge">file file</code></li>
    <li>you could look at the first few ‘magic’ bytes of the file, which spell <code class="language-plaintext highlighter-rouge">ca fe ba be</code> in hex. Crossreferencing it with <a href="https://en.wikipedia.org/wiki/List_of_file_signatures">this Wikipedia article</a> should yield us an answer!</li>
  </ul>

  <h5 id="flag-7">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{class}</code></p>
</details>

<h3 id="sicc-views-30-points">sicc views (30 points)</h3>

<h4 id="files-4">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/sicc-views/_ctfd/files/sicc%20views.jpg">sicc views.jpg</a></li>
</ul>

<details>

  <h5 id="author-8">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-8">Walkthrough</h5>
  <p>sicc views! what park was this taken at?</p>

  <p><strong>Format</strong>: Remember to wrap your answer with COMPCLUB{}, and replace any spaces with underscores.</p>

  <p>For example, if this were at Pitt Street Reserve, the flag would be <code class="language-plaintext highlighter-rouge">COMPCLUB{pitt_street_reserve}</code>.</p>

  <h5 id="flag-8">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{diamond_bay_reserve}</code></p>
</details>

<h3 id="i-have-no-words-40-points">i have no words (40 points)</h3>

<p>I have a geography essay due tomorrow and I’ve only written about 965 words but it needs to be 1100 words.</p>

<p>Luckily, my friend told me a trick that lets my essay look like it has the correct number of words (including the question). You can even check yourself by pressing Tools at the top and then Word Count.</p>

<p>I’ve also found this trick is a nice way to hide secrets including CTF flags…</p>

<p><a href="https://docs.google.com/document/d/1C7YtjaANKi_ai48Jg3hG04MZFuinmkBPFjChM7W11CE/edit?usp=sharing">link</a></p>

<details>

  <h5 id="author-9">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-9">Walkthrough</h5>
  <p>The flag, and other text have been written in white. Any sort of highlighting will expose the hidden text. The last chunk of white text contains the flag.</p>

  <h5 id="flag-9">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{d0nt_tUrN_@_bL!nD_3y3}</code></p>
</details>

<h3 id="paulas-pizzeria-50-points">Paula’s Pizzeria (50 points)</h3>

<p>I’m hungry and Paula owns one of the best pizza places in Australia. I want to go but I forgot where it was! No worries though because I put the phone number somewhere…</p>

<p>Okay, I may have lost it but I remember that I was working on the slides when I wrote it down. Can you help me find the suburb of where her pizza place is located?</p>

<p><strong>Format</strong>: Wrap your answer with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>. For example, if the suburb is Randwick, the answer would be <code class="language-plaintext highlighter-rouge">COMPCLUB{randwick}</code>.
<a href="https://docs.google.com/presentation/d/1JeATQSyBZybh7ZAEUkLF-iIG5OJ2uw0BDexhyfIoGn0/edit">link</a></p>

<details>

  <h5 id="author-10">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-10">Walkthrough</h5>
  <ol>
    <li>Look at the speaker notes to get the digits <code class="language-plaintext highlighter-rouge">9413 1120</code></li>
    <li>Search up <code class="language-plaintext highlighter-rouge">9413 1120</code> to get the Domino’s in Balcatta</li>
  </ol>

  <h5 id="flag-10">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{Balcatta}</code></p>
</details>

<h3 id="going-broke-80-points">going broke (80 points)</h3>

<p>i’ve lost track of my accounting for this month i am going to go broke.</p>

<p>respectfully,</p>

<p>i am in tears.</p>

<h4 id="files-5">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/going-broke/_ctfd/files/budget.png">budget.png</a></li>
</ul>

<details>

  <h5 id="author-11">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-11">Walkthrough</h5>

  <p>We’re provided with a PNG file, but it doesn’t appear to be a regular PNG file which we’re able to open.</p>

  <p>We could use the <code class="language-plaintext highlighter-rouge">file</code> command to determine that this is an Excel file, or look at the magic bytes, and note that it shares the same magic bytes as a <code class="language-plaintext highlighter-rouge">zip</code>, <code class="language-plaintext highlighter-rouge">apk</code>, <code class="language-plaintext highlighter-rouge">docx</code>, <code class="language-plaintext highlighter-rouge">pptx</code>, <code class="language-plaintext highlighter-rouge">xlsx</code> and many more. (All of the other file types can be treated just like zip files, and unzipped!)</p>

  <p>However, in the context of accounting, <code class="language-plaintext highlighter-rouge">xlsx</code> makes the most sense, so we can change the file extension and open the file in Excel or Google Sheets.</p>

  <p>Doing so reveals two spreadsheets. We want to go to the sheet called ‘definitely accounting’, where the flag is spread horizontally across the sheet.</p>

  <h5 id="flag-11">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{d1dNt_aCcount_f0R_tHi5}</code></p>
</details>

<h3 id="steve-the-dinosaur-90-points">Steve the Dinosaur (90 points)</h3>

<p>Steve’s a cheeky fella. He’s a stegosaurus who won’t shut up about it and because he’s vegetarian, he loves to eat noodles but never tells us his favourite type of noodles. Instead he has just sent us a selfie of his best fit and expects us to find his favourite noodle. He tells us this image is hidden inside a flag within the image. Find the flag!</p>

<h4 id="files-6">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/forensics/steve-the-dinosaur/_ctfd/files/stegosaurus.png">stegosaurus.jpg</a></li>
</ul>

<details>

  <h5 id="author-12">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-12">Walkthrough</h5>

  <p>The text has been camouflaged in with the white background.</p>

  <p>There are a couple tools that can be used to find the text:</p>
  <ul>
    <li>Using a marquee or magic wand like tool</li>
    <li>Adjusting brightness, contrast or levels</li>
  </ul>

  <h5 id="flag-12">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{r9men.!s.the.b3st.n00dle}</code> - surprisingly, I (@abiramen) didn’t write this flag.</p>
</details>

<h2 id="misc">Misc</h2>

<h3 id="slides-20-points">Slides (20 points)</h3>

<p><a href="https://docs.google.com/presentation/d/19mnBnvYvn7uF-01aQ2rBvCqVMqcIKoxQppwgRKOuXn8">Here</a> are the slides for today’s workshop. I wonder if there’s anything handy in there.</p>

<details>

  <h5 id="author-13">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-13">Walkthrough</h5>

  <p>The flag is on the slide about… flags!</p>

  <h5 id="flag-13">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{y0ur_f1rSt_fLaG}</code></p>
</details>

<h3 id="sneaky-folders-70-points">Sneaky Folders (70 points)</h3>

<p>Felix has come up with an amazing way to organise his files and folders. He has used ascii and binary to hide messages that he doesn’t want others to see and he only needs to remember a letter to find his hidden files.</p>

<p>He has hidden a flag within in his folders and has been kind enough to give us his personal key that he uses to remember where it is: <code class="language-plaintext highlighter-rouge">c</code>. See if you can find the flag!</p>

<h4 id="files-7">Files</h4>
<p><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/misc/sneaky-folders/_ctfd/files/not%20suspicious%20folder.zip">not suspicious folder.zip</a></p>

<details>

  <h5 id="author-14">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-14">Walkthrough</h5>
  <ol>
    <li>The letter ‘c’ is represented as <code class="language-plaintext highlighter-rouge">01100011</code> as an ASCII/UTF-8 byte.</li>
    <li>By corresponding ‘flag-in-here’ folders to 1 and ‘flag-not-in-here’ folders to 0, we can traverse and find the flag.</li>
  </ol>

  <p>An unintended solution by @abiramen:</p>
  <ul>
    <li><code class="language-plaintext highlighter-rouge">cd</code> to the top folder, and run <code class="language-plaintext highlighter-rouge">grep -r COMPCLUB</code> - this will <strong>r</strong>ecursively grep through all the files in the folder to find a match.</li>
  </ul>

  <h5 id="flag-14">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{n!cE_b!N@rY_s3@RcH!n9_th3r3!!!}</code></p>
</details>

<h3 id="compclub-does-countdown-75-points">CompClub Does Countdown (75 points)</h3>

<p>Abiram, Felix and Paula have decided to host a new show called ‘CompClub does Countdown’ and you have been invited to take part in the first episode. There are two sections which you must complete:</p>

<p><strong>Numbers section</strong></p>

<p>Using the following numbers <code class="language-plaintext highlighter-rouge">25, 1, 1, 3, 4, 2</code> once each, get as close as you can to the number 560 with the operations of addition, subtraction, multiplication and division.</p>

<p><strong>Letters section</strong></p>

<p>Find the English word that uses the most number of letters in the list <code class="language-plaintext highlighter-rouge">[L, N, T, E, I, E, R, H, A]</code> where each letter is only used once.</p>

<p><strong>Format</strong>: <code class="language-plaintext highlighter-rouge">COMPCLUB{[WORD][NUMBER]}</code> For example, if the correct word was ‘example’ and the closest number was 570, then the flag would be <code class="language-plaintext highlighter-rouge">COMPCLUB{example570}</code>.</p>

<details>

  <h5 id="author-15">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-15">Walkthrough</h5>
  <p>Use an anagram solver to find the word heartline, and do some maths to get:
<code class="language-plaintext highlighter-rouge">(25 * 2 + 1) * (3 * 4 - 1) = 561</code></p>

  <h5 id="flag-15">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{heartline561}</code></p>
</details>

<h3 id="not-a-reliable-source-80-points">Not A Reliable Source (80 points)</h3>

<p>Whenever I search something up, Wikipedia always comes up first - no matter what.
It’s the best place to <del>plagiarise</del> learn from but teachers always say it’s ‘not a reliable source’. Anyways, I was writing this essay on CTFs on Christmas Day, and even left a flag in my essay!</p>

<details>

  <h5 id="author-16">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-16">Walkthrough</h5>
  <ol>
    <li>Browse around pages relevant to CTFs, namely ‘Capture the flag (cybersecurity)’</li>
    <li>The flag should be in the introduction - otherwise it will be found in the edit history as most recently.</li>
  </ol>

  <h5 id="note-from-abiramen">Note from @abiramen</h5>
  <p>If you’re a future prospective beginner CTF author taking inspiration, I’m not quite sure if vandalising Wikipedia is necessarily the <em>most</em> ethical thing to do, although this wasn’t too bad since none of the information added was technically incorrect!</p>

  <h5 id="flag-16">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{y0u_h@v3_f0uND_th3_fl@G!}</code></p>
</details>

<h2 id="recon">Recon</h2>

<p>The last two challenges in this section were written up halfway through the CTF since some teams were way too quick.</p>

<h3 id="unfriendly-neighbourhood-barbecue-65-points">Unfriendly Neighbourhood Barbecue (65 points)</h3>

<p>We are always scared of computers gaining sentience but we never once considered that the beloved CSESoc and CompClub barbecue ‘Jebediah Dumptruck’ may have grown his own mind.</p>

<p>He has run rampant, creating social media accounts to advertise the rise of the barbecues. To stop him, we have to find the flag that he has hidden away in his propaganda - but beware that he has planted fake flags as well.</p>

<p>Note: You can do this without a FB account :)</p>

<details>

  <h5 id="author-17">Author</h5>
  <p>@felixc90</p>

  <h5 id="walkthrough-17">Walkthrough</h5>
  <ol>
    <li>Search up Jebediah Dumptruck and click the first Facebook profile
(Note this does not require an account as you can still look at the necessary photos)</li>
    <li>Look through a couple photos and see that it guides user to comments</li>
    <li>One comment under the profile picture suggests looking at the tags</li>
    <li>The cover photo reveals the flag is the tag ‘next to Will Fernandez’</li>
  </ol>

  <h5 id="flag-17">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{go@t3d_o5!Nt_sk!LLs}</code></p>
</details>

<h3 id="cold">cold</h3>

<p>Please help! I’m stuck out here and it’s extremely cold and I am in need of a rescue.</p>

<p>You’ll need to find me to rescue me, which begs the question of ‘where am I?’.</p>

<p><strong>Format</strong>: Wrap the name of the street which the photo was taken on with CompClub.</p>

<p>For example, if this were taken at High St, the flag would be <code class="language-plaintext highlighter-rouge">COMPCLUB{high_street}</code>.</p>

<h4 id="files-8">Files</h4>
<ul>
  <li><a href="https://github.com/abiramen/2022-compclub-summer-ctf/blob/main/recon/cold/_ctfd/files/cold.jpg">cold.jpg</a></li>
</ul>

<details>

  <h5 id="author-18">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-18">Walkthrough</h5>

  <p>There’s not much useful here in the image, besides the fact that we appear to be in a snowy country with yellow traffic lights. There’s also not much in the metadata, besides the fact that I took the photo with a Pixel 2 XL in January 2018.</p>

  <p>However, the one handy thing that we have here in the top left is a big building with a distinctive yellow logo, with a walking hand. If you’re a boomer like myself (seeing students trying to solve this challenge has made me feel old), you might instantly recognise this as the Yellow Pages logo - if not, you might have had to do some googling.</p>

  <p>From this point, you could check Google Images for ‘yellow pages building’, and you’d be presented with images of a building in Toronto, Canada which looks to be the same building! This is listed as the (now permanently closed) Yellow Pages Group Tower in Scarborough.</p>

  <p>Looking for the ‘Yellow Pages Group Tower’ in Google Maps takes us to the intersection of Milner and Progress Ave. Dropping into Street View, we can correlate this with other buildings to confirm that we are indeed on Progress Ave.</p>

  <h5 id="flag-18">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{progress_avenue}</code></p>
</details>

<h3 id="fontaine-90-points">fontaine (90 points)</h3>

<p>Saw this cool fountain a few months ago, can you name it?</p>

<p>Format: Wrap the name of the fountain with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>, replacing spaces with underscores. For example, if the fountain is the Lloyd Rees Fountain, the flag would be <code class="language-plaintext highlighter-rouge">COMPCLUB{lloyd_rees_fountain}</code>.</p>

<h4 id="files-9">Files</h4>
<ul>
  <li><a href="https://abiram.me/assets/images/blog/compclub-2022/fontaine.jpg">fontaine.jpg</a></li>
</ul>

<details>

  <h5 id="author-19">Author</h5>
  <p>@abiramen</p>

  <h5 id="notes">Notes</h5>
  <p>Again, I was really hoping to save this challenge for another CTF, but some of you were just too skilled!</p>

  <p>The reason I had to distort this image so much was to prevent it from being found via reverse image search.</p>

  <h5 id="walkthrough-19">Walkthrough</h5>

  <p>We can see that the GPS metadata for this file has been stripped. However, we can still see that the photo was taken on the 9th of April 2021, and there’s a comment attached to the file, reading ‘fountain i saw while getting lunch at hacker conf’.</p>

  <p>From this, we could take a look at hacker conferences that took places around the 9th of April 2021 with some googling. A result that pops up here is the BSides Canberra 2021 conference.</p>

  <p>We can now google for ‘canberra fountains’, giving us the Canberra Times Fountain as the first result.</p>

  <h5 id="flag-19">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{canberra_times_fountain}</code></p>
</details>

<h3 id="underpass-145-points">underpass (145 points)</h3>

<p>What’s the name of the park on the other side of the underpass?</p>

<p>Format: Replace any spaces with underscores and wrap with <code class="language-plaintext highlighter-rouge">COMPCLUB{}</code>. For example, if this were Pitt Street Reserve, the flag would be <code class="language-plaintext highlighter-rouge">COMPCLUB{pitt_street_reserve}</code>.</p>

<h4 id="files-10">Files</h4>
<ul>
  <li><a href="https://abiram.me/assets/images/blog/compclub-2022/underpass.jpg">underpass.jpg</a></li>
</ul>

<details>

  <h5 id="author-20">Author</h5>
  <p>@abiramen</p>

  <h5 id="notes-1">Notes</h5>

  <p>After hearing that all my challenges had been solved including the addition of fontaine, with an hour left, I had to pull out a very cruel challenge from my CTF bank which I was really hoping to save for O-Week CTF. Oh well, this was really fun to watch people attempt to, and struggle to (but thankfully eventually manage to) solve.</p>

  <p>Huge congrats to the two people that did manage to work this out.</p>

  <p>I highly recommend spending at least an hour looking at this image before giving up and reading through the solution below.</p>

  <h5 id="walkthrough-20">Walkthrough</h5>

  <p>From the signage, we can determine that this is likely in Sydney, with the trees also reinforcing that this is in Australia.</p>

  <p>The key thing to note is the digital sign above the road in the top left of the image. This, as well as the two separate carriageways suggests that this is likely one of Sydney’s motorways.</p>

  <p>We can also see see that there appears to be a cycleway running alongside the motorway - this is pretty common in Sydney, with the M2, M4, M5 and M7 all being associated with cycleways.</p>

  <p>The photo appears to be taken at an intersection with another path, from the positioning as well as the signage up ahead.</p>

  <p>Now would be a good time to open up Google Maps and start scanning the aforementioned motorways. From the image, we can get a rough idea of the shapes of the paths and how it interacts with the motorway. We’re trying to look for something like this:</p>

  <p><img src="/assets/images/blog/compclub-2022/underpass.jpg" alt="cursed path" /></p>

  <p>With a bit of time scanning, you should come across this path!</p>

  <h5 id="flag-20">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{lady_penhryn_park}</code></p>
</details>

<h2 id="web">Web</h2>

<h3 id="epf-184-points-across-5-challenges">epf (184 points across 5 challenges)</h3>

<p>I miss the good old days of Club Penguin’s Elite Penguin Force, so I made my own EPF.</p>

<p>You can try and become an epik penguin! There are five flags to find along the way.</p>

<p><a href="https://github.com/abiramen/2022-compclub-summer-ctf/tree/main/web/epf-0/src">source code</a></p>

<h4 id="part-0-24-points">Part 0 (24 points)</h4>

<details>

  <h5 id="author-21">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-21">Walkthrough</h5>
  <p>For this challenge, we had to inspect element or view page source (<code class="language-plaintext highlighter-rouge">Ctrl</code>+<code class="language-plaintext highlighter-rouge">U</code>) on the login page, where we can spot a HTML comment containing the flag.</p>

  <h5 id="flag-21">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{g00D_p3NguIns_cH3cK_p4ge_s0urCe}</code></p>

</details>

<h4 id="part-1-25-points">Part 1 (25 points)</h4>

<details>

  <h5 id="author-22">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-22">Walkthrough</h5>
  <p>The HTML page source also linked to a script at <code class="language-plaintext highlighter-rouge">static/hello.js</code>. We can spot a flag in the comments of this file too.</p>

  <h5 id="flag-22">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{sCr!pTiNg_penGu1Ns!}</code></p>

</details>

<h4 id="part-2-30-points">Part 2 (30 points)</h4>

<details>

  <h5 id="author-23">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-23">Walkthrough</h5>
  <p>Check for any cookies which have been set, typically under the Application &gt; Cookies tab in Developer Tools on most browsers.</p>

  <h5 id="flag-23">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{p3NgU1N_L0Ve$_c0oK1ez}</code></p>

</details>

<h4 id="part-3-45-points">Part 3 (45 points)</h4>

<details>

  <h5 id="author-24">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-24">Walkthrough</h5>
  <p>Check the <code class="language-plaintext highlighter-rouge">/robots.txt</code> file for the site.</p>

  <h5 id="flag-24">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{r0bot!_penGuiN!_f0rCe?!}</code></p>

</details>

<h4 id="part-4-55-points">Part 4 (55 points)</h4>

<details>

  <h5 id="author-25">Author</h5>
  <p>@abiramen</p>

  <h5 id="walkthrough-25">Walkthrough</h5>
  <p>Using the md file you found in the robots.txt in previous challenge, determine that we must have a username with at least 10 characters to become an epik penguin. However, the login form doesn’t appear to allow us to enter more than 10 characters. To get around this, we can edit or remove the <code class="language-plaintext highlighter-rouge">maxlength</code> attribute from the <code class="language-plaintext highlighter-rouge">&lt;input&gt;</code> element on the form.</p>

  <h5 id="flag-25">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">COMPCLUB{h4cKeR_pEnGu1N_#aCk3R_p3NgUin!!}</code></p>

</details>

<h1 id="conclusion">Conclusion</h1>

<p>On the day, we received 1159 attempted flag submissions amongst 45 competitors in 8 teams, of which, 249 of submissions were correct.</p>

<p>Thanks to all the students that came along and participated in workshops and the CTF! I hope to see you all next year, when we can hopefully actually run this event in person.</p>

<p>Also a shoutout again to Paula and Felix, as well as all the mentors who volunteered throughout the week, as well as <a href="https://github.com/N-Tandiono">Nicholas</a> who has tested my recon images in the past.</p>]]></content><author><name></name></author><category term="security" /><category term="ctf" /><summary type="html"><![CDATA[After running CompClub’s Intro to Security workshop for some eager high schoolers last year, I just had to come back to redevelop the workshop and present again.]]></summary></entry><entry><title type="html">CSE and VS Code</title><link href="/cse-setup" rel="alternate" type="text/html" title="CSE and VS Code" /><published>2021-08-28T00:00:00+00:00</published><updated>2021-08-28T00:00:00+00:00</updated><id>/cse-setup</id><content type="html" xml:base="/cse-setup"><![CDATA[<p>We’re going to be setting up a text editor called Visual Studio Code, or VS Code, to remotely work on the CSE computers without needing a VNC client!</p>

<h2 id="why-are-we-doing-this">Why are we doing this?</h2>
<p>You’ve likely used a program called TigerVNC to connect to the School of CSE’s computers before, which provides you a visual interface with a full desktop environment. f you’re currently doing COMP1511, I would strongly advise you to use the provided desktop environment and gedit over TigerVNC for the first week, just to familiarise yourself with it.</p>

<p>The solution we’re about to go through provides a more seamless coding experience without having to load a full visual interface from CSE computers. This has the added benefits of not being affected by compression, especially if you have a poor connection, and being able to use a more fully fledged editor.</p>

<p class="alert alert-red"><em><strong>obligatory disclaimer</strong>: despite myself (and those acknowledged in this post) being employed by the school of cse at the time of writing, this does <strong>not</strong> necessarily reflect the views of my employer. i am also not responsible if your computer blows up. you get it, don’t you?</em> 👌👌</p>

<h2 id="getting-started-">Getting started 🎉</h2>
<p>First of all, we’re going to acquaint ourselves with Visual Studio Code, or VS Code, a text editor.</p>

<ol>
  <li><strong>Download and install Visual Studio Code.</strong> You can find a download <a href="https://code.visualstudio.com/download" target="_blank">here</a>.</li>
  <li><strong>If prompted to do so during the installation process</strong>, select the checkbox which says ‘Add to PATH’.</li>
  <li><strong>Restart your computer once you’re done.</strong></li>
</ol>

<p>You should now have VS Code installed! You can now edit files on your own computer, but that’s not quite what we want - our goal is to edit files on CSE machines.</p>

<h2 id="our-friend-ssh-">Our friend, SSH 🙃</h2>
<p>Some cool computer scientists invented a way to allow you to communicate with remote computers over the internet called SSH, or Secure Shell. This allows us to edit files on another computer, such as the CSE servers, and run commands through a terminal on CSE servers.</p>

<p>Luckily for us, we can add an extension to VS Code to use SSH to connect to CSE machines.</p>

<p class="alert">This guide is particularly intended for first year CSE students, especially if you’re taking COMP1511, COMP1521 and COMP2521. If you’re a more experienced user with your own local setup, and have other requirements or prefer to have a bit more control over everything, check out this guide <a href="/cse-sshfs" target="_blank">here</a>.</p>

<h3 id="part-1-installing-the-extension">Part 1: Installing the extension</h3>
<ol>
  <li><strong>Open VS Code.</strong></li>
  <li><strong>Open the Extensions menu.</strong> You can access it using <code class="language-plaintext highlighter-rouge">Ctrl+Shift+X</code>.</li>
  <li><strong>Search for and install the SSH FS extension.</strong> Once this is done, you might be prompted to reload the window. <sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></li>
</ol>

<table>
  <thead>
    <tr>
      <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-install.png" alt="Installing the SSH FS extension" /></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Installing the SSH FS extension.</td>
    </tr>
  </tbody>
</table>

<h3 id="part-2-configuring-to-connect-to-cse">Part 2: Configuring to connect to CSE</h3>

<p>Now that we’ve installed, there should be a new icon in the menu bar on the left of your VS Code window.</p>

<p class="alert">On macOS? Use the <kbd>Cmd</kbd> key instead of <kbd>Ctrl</kbd>.</p>

<ol>
  <li><strong>Select the newly added SSH FS icon</strong>. This is the menu button in blue in the below image.</li>
  <li>
    <p><strong>Under the Configuration section</strong>, select the button to create a new configuration.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-add-config.png" alt="Adding a new configuration under the SSH FS menu" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">Adding a new configuration under the SSH FS menu.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li>
    <p><strong>Give the configuration a name</strong>, and click save. A good name might be ‘cse’.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-new-config.png" alt="Creating the configuration" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">Creating the new configuration.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li><strong>We’ll now need to edit this configuration</strong>. In the new screen that pops up, fill out the following options:
    <ul>
      <li><strong>Host</strong>: Set this to be <code class="language-plaintext highlighter-rouge">login.cse.unsw.edu.au</code>.</li>
      <li><strong>Port</strong>: This should be set to <code class="language-plaintext highlighter-rouge">22</code>.</li>
      <li><strong>Root</strong>: Set this to <code class="language-plaintext highlighter-rouge">~</code>, which is the key that’s generally above the tab key on most keyboards.</li>
      <li><strong>Username</strong>: This should be your zID, for example, <code class="language-plaintext highlighter-rouge">z5123456</code>.</li>
    </ul>

    <p><strong>Leave everything else blank</strong>, scroll down, and <strong>click save</strong>.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-sample-config.png" alt="Example configuration" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">A sample configuration for z5123456.</td>
        </tr>
      </tbody>
    </table>
  </li>
</ol>

<h3 id="part-3-using-our-new-configuration-to-connect">Part 3: Using our new configuration to connect!</h3>

<p>We’re finally at the part where we get to connect to CSE machines.</p>

<ol>
  <li><strong>Head back to the SSH FS menu</strong>. Our new CSE configuration should be listed.</li>
  <li>
    <p><strong>Click the button to ‘add as workspace folder’</strong>.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-add-workspace.png" alt="Add as workspace folder" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">Add CSE as a workspace folder.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li><strong>Enter your UNSW password when prompted</strong>.</li>
</ol>

<p>Your CSE files should now all be visible in the sidebar! You can double-click to open them and edit them, or use <kbd>Ctrl</kbd>+<kbd>N</kbd> and <kbd>Ctrl</kbd>+<kbd>S</kbd> to create new files.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-see-files.png" alt="See your files!" /></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">We should now be able to see our CSE files!</td>
    </tr>
  </tbody>
</table>

<p>There’s a good chance that you also need a <strong>terminal</strong> to get your work done. To do this,</p>

<ol>
  <li><strong>Head back to the SSH FS menu</strong>, where our CSE configuration is listed.</li>
  <li><strong>Click the ‘open remote SSH terminal’ button</strong>. A terminal pane should now open. You can use commands like <code class="language-plaintext highlighter-rouge">cd</code> and <code class="language-plaintext highlighter-rouge">ls</code> just as you’re used to. Instead of using <code class="language-plaintext highlighter-rouge">gedit</code> now, we can create new files inside VS Code.</li>
</ol>

<table>
  <thead>
    <tr>
      <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-open-terminal.png" alt="See your files!" /></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">This button should launch a terminal on the bottom of our window.</td>
    </tr>
  </tbody>
</table>

<p>You can open as many new terminals as you want.</p>

<h3 id="part-4-optional-using-the-code-command-to-open-files">Part 4 (optional): Using the <code class="language-plaintext highlighter-rouge">code</code> command to open files</h3>

<p>We can enable a setting to allow us to use the <code class="language-plaintext highlighter-rouge">code</code> command in SSH terminal to open files (a bit like using the <code class="language-plaintext highlighter-rouge">gedit</code> command to open a new window containing a file):</p>

<ol>
  <li><strong>Open the Settings menu.</strong> This can be accessed by using <kbd>Ctrl</kbd>+<kbd>,</kbd>, or the Settings icon in the bottom left of your window.</li>
  <li>
    <p><strong>Search for the ‘sshfs flags’ setting</strong>, and select <em>‘Edit in settings.json’</em>.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-flags.png" alt="Searching for the SSH FS flags setting." /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">The following option should show up in our search.</td>
        </tr>
      </tbody>
    </table>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-json-flags.png" alt="The SSH FS flags settings in JSON" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">A new tab should open, presenting an empty set of square brackets like this. There may or may not be a comma there.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li>
    <p><strong>Add the following in between the square brackets</strong>: <code class="language-plaintext highlighter-rouge">"REMOTE_COMMANDS"</code> (be sure to include the quotation marks), and use <kbd>Ctrl</kbd>+<kbd>S</kbd> to save the file.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-added-flag.png" alt="The &quot;REMOTE_COMMANDS&quot; flag added." /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">You should now have this in between the square brackets. Don’t add a comma if there wasn’t already one there.</td>
        </tr>
      </tbody>
    </table>
  </li>
</ol>

<p>You should now be able to use the <code class="language-plaintext highlighter-rouge">code</code> command in your terminal the next time you connect.</p>

<h2 id="bonus-coins-avoiding-the-need-to-type-your-password-each-time">Bonus coins: Avoiding the need to type your password each time</h2>

<p>Being prompted to enter our password each time we connect is slightly annoying. Storing our password in the config file isn’t ideal either, so our solution involves using a magic pair of keys.</p>

<p>(If you’ve done this before as part of the old guide, feel free to skip to <a href="#part-7-telling-the-ssh-fs-extension-to-use-your-new-keys" class="page-anchor-offset">Part 7</a> in this section.)</p>

<h3 id="part-5-generating-a-keypair">Part 5: Generating a keypair</h3>
<h5 id="windows-10">Windows 10</h5>
<ol>
  <li><strong>Open Windows Powershell.</strong> You can do this by entering <code class="language-plaintext highlighter-rouge">powershell</code> in Search.</li>
  <li><strong>Generate a keypair.</strong>  Use the command <code class="language-plaintext highlighter-rouge">ssh-keygen -t ed25519</code> in Powershell.</li>
  <li><strong>You’ll be asked where you want to save the key.</strong> Leave this blank, and press <kbd>Enter</kbd>.</li>
  <li><strong>You’ll be asked to set a password.</strong> Leave this blank, and press <kbd>Enter</kbd> again. Do the same when you are asked to confirm it.</li>
</ol>

<h5 id="linuxmacos">Linux/macOS</h5>

<ol>
  <li><strong>Open the Terminal.</strong></li>
  <li><strong>Generate a keypair.</strong>  Use the command <code class="language-plaintext highlighter-rouge">ssh-keygen -t ed25519</code>.</li>
  <li><strong>You’ll be asked where you want to save the key.</strong> Leave this blank, and press <kbd>Enter</kbd>.</li>
  <li><strong>You’ll be asked to set a password.</strong> Leave this blank, and press <kbd>Enter</kbd> again. Do the same when you are asked to confirm it.</li>
</ol>

<p>Your computer has now generated two files: <code class="language-plaintext highlighter-rouge">id_ed25519</code> and <code class="language-plaintext highlighter-rouge">id_ed2559.pub</code>. The <code class="language-plaintext highlighter-rouge">id_ed25519.pub</code> file is what’s called a public key, while <code class="language-plaintext highlighter-rouge">id_ed25519</code> is a private key. <strong>Never share (or really, even open) your private key.</strong> It’s called a private key for a reason <del>(that is, because you should share it with the world)</del>.</p>

<h3 id="part-6-using-the-keypair-on-cse-machines">Part 6: Using the keypair on CSE machines</h3>
<p>For this to work, we need to give the CSE machines the <strong>public</strong> half of your keypair. If you’re on Linux/macOS, this should be pretty easy, but if you’re on Windows 10, there’s a few more steps.</p>

<h5 id="windows-10-1">Windows 10</h5>
<ol>
  <li><strong>Open Powershell</strong>, just like in Part 4. You can use the same window if you still have it open.</li>
  <li>
    <p><strong>Run the following command in Powershell</strong>, replacing with your own zID. Enter your password when prompted. (Yes, the entire thing should be one command.)</p>

    <p><code class="language-plaintext highlighter-rouge">ssh z5555555@cse.unsw.edu.au "mkdir ~/.ssh;chmod 700 ~/.ssh;touch ~/.ssh/authorized_keys;chmod 600 ~/.ssh/authorized_keys"</code></p>
  </li>
  <li>
    <p><strong>Run the following command</strong>, again replacing with your own zID. Enter your password when prompted. (Again, this is all one command, so be sure to copy-paste the whole thing!)</p>

    <p><code class="language-plaintext highlighter-rouge">type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh z5555555@cse.unsw.edu.au "cat &gt;&gt; .ssh/authorized_keys"</code></p>
  </li>
  <li><strong>Attempt to connect</strong> with <code class="language-plaintext highlighter-rouge">ssh z5555555@cse.unsw.edu.au</code>, replacing with your own zID. If we were successful, you should <strong>not</strong> be prompted for your password!</li>
</ol>

<h5 id="linuxmacos-1">Linux/macOS</h5>
<ol>
  <li><strong>Open the Terminal.</strong></li>
  <li>
    <p><strong>Run the following command</strong>, replacing with your own zID.</p>

    <p><code class="language-plaintext highlighter-rouge">ssh-copy-id z5555555@cse.unsw.edu.au</code></p>
  </li>
  <li><strong>Enter your password.</strong> You’ll be asked for your CSE password one last time, for the key to be copied over.</li>
  <li><strong>Attempt to connect</strong> with <code class="language-plaintext highlighter-rouge">ssh z5555555@cse.unsw.edu.au</code>, replacing with your own zID. If we were successful, you should <strong>not</strong> be prompted for your password!</li>
</ol>

<p>Voila! You had it a bit easier here.</p>

<h3 id="part-7-telling-the-ssh-fs-extension-to-use-your-new-keys">Part 7: Telling the SSH FS extension to use your new keys</h3>

<ol>
  <li><strong>Open the SSH FS menu in VS Code</strong>, yet again.</li>
  <li>
    <p><strong>Select the ‘edit configuration’ button</strong> for your CSE configuration.</p>

    <table>
      <thead>
        <tr>
          <th style="text-align: center"><img src="/assets/images/blog/cse-setup/sshfs-edit.png" alt="Editing the CSE configuration" /></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td style="text-align: center">Editing the CSE configuration.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li><strong>Scroll down</strong> to the ‘private key’ option.
    <ul>
      <li>If you’re on <strong>Windows</strong>, set this to <code class="language-plaintext highlighter-rouge">$USERPROFILE/.ssh/id_ed25519</code>.</li>
      <li>If you’re on <strong>macOS or Linux</strong>, set this to <code class="language-plaintext highlighter-rouge">$HOME/.ssh/id_ed25519</code>.</li>
    </ul>
  </li>
  <li><strong>Be sure to scroll down and click save.</strong></li>
  <li><strong>Connect again</strong>, just like in Part 3. You shouldn’t be prompted for your password now!</li>
</ol>

<h3 id="other-boring-stuff-starting-in-a-different-folder">Other (boring) stuff: Starting in a different folder</h3>

<p>If you hate clutter and don’t want to your entire CSE home directory (say, for example, you have a folder <code class="language-plaintext highlighter-rouge">~/comp1511</code> in which you do all your work in), you can edit your SSH FS configuration to start in a different <strong>root</strong> directory, such as <code class="language-plaintext highlighter-rouge">~/comp1511</code> instead of <code class="language-plaintext highlighter-rouge">~</code>, or even just make a whole new configuration for this.</p>

<h2 id="making-vs-code-even-cooler">Making VS Code even cooler</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center"><img src="/assets/images/blog/cse-setup/my-vscode.png" alt="My VS Code" /></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">I personally don’t code on CSE machines, but my VS Code looks like this!</td>
    </tr>
  </tbody>
</table>

<h3 id="editor-rulers">Editor rulers</h3>
<p>The COMP1511 style guide requires you to make sure your lines of code aren’t longer than 80 characters (and it’s good practice to do so!). You can make VS Code show a handy little ruler to make sure your lines aren’t too long.</p>

<ol>
  <li><strong>Open the settings.json file.</strong> To do this, press <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>P</kbd> and search for <strong>Open Settings (JSON).</strong> A file should open.</li>
  <li><strong>We need to add to this file.</strong> You should see that the last line is a curly brace like this: <code>}</code>. What we want to do is add a comma to the second last line. Then, create a new line after the comma, and paste this:
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"[c]" :{
 "editor.rulers" : [
     80
 ]
}
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="other-extensions">Other extensions</h3>
<p>VS Code has heaps of themes and extensions. Some which I use:</p>
<ul>
  <li><strong>Material Icon Theme.</strong></li>
  <li><strong>Dainty and Dainty - Material Theme.</strong> I personally use Dainty - Material Theme Ocean.</li>
  <li><strong>Discord Rich Presence.</strong> If you use Discord, then it’ll update your currently playing status with details of what you’re coding on. If one of your friends tries to drag you into a voice call, you can just open up VS Code and tell them that you’re busy working 😛</li>
</ul>

<p>It’s worth noting that several extensions may not work due to the nature of how the SSH FS extension works - if there’s a particular extension you need, and you’re a more advanced user, try following my <a href="/cse-sshfs">other guide</a> instead.</p>

<h2 id="troubleshooting-ssh-fs">Troubleshooting SSH FS</h2>

<h3 id="error-running-command-sshfsadd">Error running command sshfs.add</h3>

<p>Be sure that you’ve used the correct name for the Host under your configuration, such as <code class="language-plaintext highlighter-rouge">login0.cse.unsw.edu.au</code> for example. If this doesn’t work, try using <code class="language-plaintext highlighter-rouge">login.cse.unsw.edu.au</code>, or <code class="language-plaintext highlighter-rouge">cse.unsw.edu.au</code> instead.</p>

<h2 id="have-feedback-or-want-to-reach-out">Have feedback, or want to reach out?</h2>
<p>If you’ve spotted an error, or have any suggestions or improvements that I can make, feel free to contact me! My details are at the bottom of this page.</p>

<h2 id="acknowledgements">Acknowledgements</h2>
<ul>
  <li><a href="https://github.com/spanishpear" target="_blank">spanishpear</a> for letting me know that CSE intends to encouage students to use this extension, and posting the original GitHub issue which led to the <code class="language-plaintext highlighter-rouge">code</code> command getting added, leading to me unshelving this blogpost idea.</li>
  <li><a href="https://github.com/hexDoor">hexDoor</a> for discovering and filing an issue with the SSH FS extension’s method of determining startup directory.</li>
  <li>The maintainers and contributors of relevant open source software, particularly <a href="https://github.com/SchoofsKelvin">SchoofsKelvin</a> for their particular quick response to issues raised by the above people and myself.</li>
  <li><a href="https://github.com/adaluong" target="_blank">adaluong</a> and <a href="https://github.com/lukefisklennon" target="_blank">lukefisklennon</a> for reading over this post and providing feedback.</li>
</ul>

<p>Again, this post does not necessarily represent the views of the School of CSE. CSE’s official guide can be found <a href="https://www.cse.unsw.edu.au/~learn/homecomputing/sshfs-remote/" target="_blank">here</a>.</p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:2">
      <p>If you’re a WSL user, it’s probably better to do this in your regular VS Code workspace, or check out the more advanced version <a href="/cse-sshfs" target="_blank">here</a>. <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="unsw-cse" /><category term="guide" /><category term="vscode" /><summary type="html"><![CDATA[We’re going to be setting up a text editor called Visual Studio Code, or VS Code, to remotely work on the CSE computers without needing a VNC client!]]></summary></entry><entry><title type="html">CSE and SSHFS</title><link href="/cse-sshfs" rel="alternate" type="text/html" title="CSE and SSHFS" /><published>2021-08-12T00:00:00+00:00</published><updated>2021-08-12T00:00:00+00:00</updated><id>/sshfs</id><content type="html" xml:base="/cse-sshfs"><![CDATA[<p class="alert">If you’re a VS Code user who is in first year, or isn’t too used to coding locally, you might be better off checking the easier method of using SSHFS with VS Code <a href="/cse-setup">here</a>.</p>

<p>Hello! About a year and a half ago, I wrote a blogpost about setting up Visual Studio Code to connect to CSE machines using the ‘Remote - SSH’ extension.</p>

<p>In hindsight, this was a <strong>terrible mistake</strong> with consequences I could not have foreseen. It turns out that the extension spawns a nodejs server on the remote machine for each connection, which requires an <em>intense</em> amount of resources <sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>.</p>

<p>CSE also has a lot of students, meaning you are not going to be having a good day if your hostname is <em>corelli</em> or <em>mandolin</em> 📉📉<sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>. I apologise for the terrible atrocities which I have inflicted upon the servers of the School of Computer Science and Engineering 🙏🥺.</p>

<p>Anyhow, this guide steps through setting up SSHFS to mount and access your CSE filesystem from your local file system! Doing so allows you to access files on CSE machines as if they are on your own computer, and automatically synchronises any changes to files. This method likely uses less than a tenth of the resources required by the VS Code remote extension!</p>

<h2 id="who-is-this-guide-for">Who is this guide for?</h2>

<p>If you’ve reached the end of the first year of your CSE degree, you <strong>probably</strong> should have a local coding setup by now, whether that be:</p>
<ul>
  <li>a native Linux (or *nix) setup, or</li>
  <li>some form of macOS setup, or</li>
  <li>WSL 2 <sup id="fnref:3"><a href="#fn:3" class="footnote" rel="footnote" role="doc-noteref">3</a></sup></li>
</ul>

<p>In that case, I highly recommend using SSHFS if you find yourself needing to work with files directly on CSE machines. If you’re in first year and feeling adventurous, you can take a try at setting it up too, but you’re probably better off following <a href="/cse-setup">this guide</a>. <sup id="fnref:6"><a href="#fn:6" class="footnote" rel="footnote" role="doc-noteref">4</a></sup></p>

<h2 id="getting-started-">Getting started 🎉</h2>

<p><em><strong>obligatory disclaimer</strong>: despite being employed by cse at the time of writing, this does <strong>not</strong> reflect the views of my employer. you get it, don’t you?</em> 👌👌</p>

<p>Now that that’s out of the way, we’ll need to install SSHFS to get started.</p>

<h3 id="linux-and-wsl-2">Linux and WSL 2</h3>

<p>I’m going to assume that you’re using an Ubuntu or Debian-based installation (this is most WSL distributions by default), in which case this should be nice and simple. If you’re not, you’re probably familiar enough with your distro’s package manager, or can grab the installer from GitHub <a href="https://github.com/libfuse/sshfs" target="_blank">here</a>.</p>

<p>Otherwise, all you’ll need to do is <strong>open a terminal</strong> and run <code class="language-plaintext highlighter-rouge">sudo apt install -y openssh-client sshfs</code> on your own computer.</p>

<h3 id="macos">macOS</h3>
<ol>
  <li><strong>Install Homebrew</strong> if you don’t have it already by following the instructions <a href="https://brew.sh" target="_blank">here</a>.</li>
  <li><strong>Install MacFUSE</strong> Run <code class="language-plaintext highlighter-rouge">brew install --cask macfuse</code> in your terminal. Go to System Preferences → Security &amp; Privacy, enable the new system extension, then restart.</li>
  <li><strong>Install sshfs</strong> Run <code class="language-plaintext highlighter-rouge">brew install gromgit/fuse/sshfs-mac</code> in your terminal and follow any steps which pop up.</li>
</ol>

<h2 id="mounting-your-cse-home-directory">Mounting your CSE home directory</h2>

<p>Our goal is to be able to access our CSE files from our own computer. To do this, we’ll need to use SSHFS to <strong>mount</strong> them.</p>

<ol>
  <li><strong>If this is your first time, make a directory</strong> in your local home directory by running <code class="language-plaintext highlighter-rouge">mkdir ~/cse</code> from a terminal. If you want to use a different folder name, feel free to do so, but make sure to use it instead in any future commands. It’s important that this is an empty folder.</li>
  <li><strong>Run the following command:</strong> <code class="language-plaintext highlighter-rouge">sshfs -o idmap=user -C z5555555@loginX.cse.unsw.edu.au: ~/cse</code>, replacing with your own zID, and replacing the X with the last digit of your zID. For example, if your zID was z5123456, you’d use <code class="language-plaintext highlighter-rouge">login6.cse.unsw.edu.au</code>.</li>
</ol>

<p>Yay! Your CSE files should now be accessible in the <code class="language-plaintext highlighter-rouge">~/cse</code> directory! You can now access them with the text editor of your choice! For example, if you’re a VS Code user, you can run <code class="language-plaintext highlighter-rouge">code ~/cse</code> to open a new workspace <sup id="fnref:4"><a href="#fn:4" class="footnote" rel="footnote" role="doc-noteref">5</a></sup>.</p>

<p><strong>Once you’re done working</strong>, you should unmount the CSE filesystem. To do this, run <code class="language-plaintext highlighter-rouge">fusermount -u ~/cse</code> on Linux, or <code class="language-plaintext highlighter-rouge">unmount -f ~/cse</code> on macOS<sup id="fnref:5"><a href="#fn:5" class="footnote" rel="footnote" role="doc-noteref">6</a></sup>.</p>

<p><img src="/assets/images/blog/cse-sshfs/sshfs-workflow.png" alt="Your SSHFS workflow, although you won't need to make a directory each time" /></p>

<p>In order to get a shell on CSE machines and run commands such as <code class="language-plaintext highlighter-rouge">autotest</code> and <code class="language-plaintext highlighter-rouge">give</code>, you’ll need to run <code class="language-plaintext highlighter-rouge">ssh z5555555@loginX.cse.unsw.edu.au</code>, (again, with your own zID, and replacing X with the last digit of your zID) and type in ‘yes’ if any prompts appear. Use <kbd>Ctrl</kbd>+<kbd>D</kbd> to exit when you’re done.</p>

<p><img src="/assets/images/blog/cse-sshfs/ssh-in.png" alt="Getting a shell" /></p>

<h2 id="setting-up-ssh-keys">Setting up SSH keys</h2>

<p>If you’re being prompted for your password whenever using SSHFS or SSH, setting up a pair of SSH keys (if you haven’t already) is probably a good idea. You can do this by following <strong>Part 4 and 5</strong> of the ‘SSH and SSH keys’ section <a href="https://www.abiram.me/cse-setup#bonus-coins-avoiding-the-need-to-type-your-password-each-time">here</a>.</p>

<h2 id="aliases">Aliases</h2>
<p>Typing in all these commands gets a bit cumbersome. We can use aliases to simplify this process a bit for us!</p>

<h3 id="determining-your-shell">Determining your shell</h3>
<p>Before we can figure out where to keep our aliases, we’ll need to figure out what shell we’re doing. In order to do this, run <code class="language-plaintext highlighter-rouge">echo $SHELL</code>.</p>

<p>It’s likely that your shell is either bash or zsh (if not, you’re probably cluey enough to figure the rest out yourself). If you’re on bash, open or create the file <code class="language-plaintext highlighter-rouge">~/.bashrc</code>. If you’re on zsh, open or create <code class="language-plaintext highlighter-rouge">~/.zshrc</code> in the text editor of your choice (eg. <code class="language-plaintext highlighter-rouge">code ~/.zshrc</code>).</p>

<h3 id="adding-our-aliases">Adding our aliases</h3>
<p>Create a newline at the end of the file you just opened, and paste in the following, being sure to replace with your own zID (also make sure to <strong>not</strong> add a space after the equals sign):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Your zID (change this)</span>
<span class="nv">_SSHFS_ZID</span><span class="o">=</span>z5555555
<span class="c"># Your desired mountpoint for your CSE home directory</span>
<span class="nv">_SSHFS_CSE_MOUNT</span><span class="o">=</span><span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/cse"</span>

<span class="nb">alias </span><span class="nv">csemnt</span><span class="o">=</span><span class="s2">"sshfs -o idmap=user -C </span><span class="k">${</span><span class="nv">_SSHFS_ZID</span><span class="k">}</span><span class="s2">@login</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span>:<span class="p"> -1</span><span class="k">}</span><span class="s2">.cse.unsw.edu.au: </span><span class="k">${</span><span class="nv">_SSHFS_CSE_MOUNT</span><span class="k">}</span><span class="s2">"</span>
<span class="nb">alias </span><span class="nv">cseumount</span><span class="o">=</span><span class="s2">"fusermount -zu </span><span class="k">${</span><span class="nv">_SSHFS_CSE_MOUNT</span><span class="k">}</span><span class="s2">"</span>

<span class="k">function </span>cse<span class="o">()</span> <span class="o">{</span>
    <span class="c"># determine where we are relative to the mountpoint (thanks @ralismark)</span>
    <span class="nb">local </span><span class="nv">rel</span><span class="o">=</span><span class="k">${</span><span class="nv">PWD</span><span class="p">##</span><span class="k">${</span><span class="nv">_SSHFS_CSE_MOUNT</span><span class="k">}}</span>

    <span class="k">if</span> <span class="o">[</span> <span class="nt">-z</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
        <span class="c"># if we don't have arguments, we give the user a shell on the remote cse machine.</span>
        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$PWD</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"</span><span class="nv">$rel</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
            <span class="c"># in the case that we're not in our mountpoint, provide a shell in their home directory.</span>
            ssh <span class="s2">"</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span><span class="k">}</span><span class="s2">@login</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span>:<span class="p"> -1</span><span class="k">}</span><span class="s2">.cse.unsw.edu.au"</span>
        <span class="k">else</span>
            <span class="c"># if within the mountpoint, cd to the equivalent dir on the remote before providing a shell (thanks @ralismark)</span>
            ssh <span class="s2">"</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span><span class="k">}</span><span class="s2">@login</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span>:<span class="p"> -1</span><span class="k">}</span><span class="s2">.cse.unsw.edu.au"</span> <span class="nt">-t</span> <span class="s2">"cd </span><span class="si">$(</span><span class="nb">printf</span> <span class="s2">"%q"</span> <span class="s2">"./</span><span class="nv">$rel</span><span class="s2">"</span><span class="si">)</span><span class="s2">; exec </span><span class="se">\$</span><span class="s2">SHELL -l"</span>
        <span class="k">fi
    else</span>
        <span class="c"># if we have arguments, we have a command to execute.</span>
        <span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$PWD</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"</span><span class="nv">$rel</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then</span>
            <span class="c"># in the case that we're not in our mountpoint, we'll execute in the home directory.</span>
            ssh <span class="nt">-qt</span> <span class="s2">"</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span><span class="k">}</span><span class="s2">@login</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span>:<span class="p"> -1</span><span class="k">}</span><span class="s2">.cse.unsw.edu.au"</span> <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span>
        <span class="k">else</span>
            <span class="c"># if within the mountpoint, cd to the equivalent dir on the remote before executing (thanks @ralismark)</span>
            ssh <span class="s2">"</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span><span class="k">}</span><span class="s2">@login</span><span class="k">${</span><span class="nv">_SSHFS_ZID</span>:<span class="p"> -1</span><span class="k">}</span><span class="s2">.cse.unsw.edu.au"</span> <span class="nt">-qt</span> <span class="s2">"cd </span><span class="si">$(</span><span class="nb">printf</span> <span class="s2">"%q"</span> <span class="s2">"./</span><span class="nv">$rel</span><span class="s2">"</span><span class="si">)</span><span class="s2"> &amp;&amp; </span><span class="si">$(</span><span class="nb">printf</span> <span class="s2">"%q "</span> <span class="s2">"</span><span class="nv">$@</span><span class="s2">"</span><span class="si">)</span><span class="s2">"</span>
        <span class="k">fi
    fi</span>
<span class="o">}</span>
</code></pre></div></div>

<p>You should now be able to open a new terminal (or run <code class="language-plaintext highlighter-rouge">exec bash</code> or <code class="language-plaintext highlighter-rouge">exec zsh</code> depending on which shell you use) and be able to enter <code class="language-plaintext highlighter-rouge">csemnt</code> to mount your CSE filesystem, and <code class="language-plaintext highlighter-rouge">cseumount</code> to unmount. You can also use <code class="language-plaintext highlighter-rouge">cse</code> to get a shell on CSE machines, and even directly run a single command by using <code class="language-plaintext highlighter-rouge">cse 1521 spim -f factorial.s</code> for example. It’s worth noting that Git operations are particularly slow through SSH FS, so it’s better to get a shell using <code class="language-plaintext highlighter-rouge">cse</code> and perform all your Git operations there.</p>

<p><img src="/assets/images/blog/cse-sshfs/alias.png" alt="Using aliases yeet" /></p>

<p>If this doesn’t work, and you’re on bash, make sure to check the relevant part of the troubleshooting section below.</p>

<h2 id="troubleshooting">Troubleshooting</h2>
<h4 id="mountpoint-not-empty">mountpoint not empty</h4>
<p>This suggests that you’re trying to mount to the <code class="language-plaintext highlighter-rouge">~/cse</code> directory, despite it currently containing files in it, or perhaps being already mounted. If it’s the former, remove these files or move them somewhere else if you need them.</p>
<h4 id="codeexe-invalid-argument">Code.exe: Invalid argument</h4>
<p>There’s a weird WSL bug where the <code class="language-plaintext highlighter-rouge">code</code> command may not work if you’re in the <code class="language-plaintext highlighter-rouge">~/cse</code> directory, so you may need to run it from outside - that is, try <code class="language-plaintext highlighter-rouge">cd ~ &amp;&amp; code ~/cse/path/to/file-or-directory</code>.</p>
<h4 id="aliases-in-bashrc-dont-work">Aliases in <code class="language-plaintext highlighter-rouge">.bashrc</code> don’t work</h4>
<p>There’s a chance that your terminal isn’t set up to automatically source <code class="language-plaintext highlighter-rouge">.bashrc</code>. To fix this, open <code class="language-plaintext highlighter-rouge">~/.profile</code>, add a newline to the end of the file and paste the following in:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="o">[</span> <span class="nt">-n</span> <span class="s2">"</span><span class="nv">$BASH_VERSION</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    if</span> <span class="o">[</span> <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.bashrc"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">source</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.bashrc"</span>
    <span class="k">fi
fi</span>
</code></pre></div></div>

<h2 id="have-feedback-or-want-to-reach-out">Have feedback, or want to reach out?</h2>
<p>If you’ve spotted an error, or have any suggestions or improvements that I can make, feel free to contact me! You can find details at the bottom of this page.</p>

<h2 id="acknowledgements">Acknowledgements</h2>
<p>While I’ve vaguely known SSHFS has existed for a bit, I never thought to use it until Nicholas Berridge-Argent published a guide to using SSHFS with CSE <a href="https://nickba.dev/misc/cse-sshfs.html">here</a>. I’ve been using SSHFS ever since, although the only times I really use CSE machines is if I need to demo something on SPIM. This guide merely expands on Nick’s guide, and a massive shoutout goes to him here!</p>

<p>Thanks to <a href="https://github.com/ralismark">@ralismark</a> for their useful feedback (such as pointing out lazy unmounting exists), and for their scripts to easily run commands and get a shell on the remote machine.</p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>This gets worse as you add more extensions. We once spotted someone using a Java language extension eating up nearly a gigabyte of RAM on its own. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p><a href="https://twitter.com/jashankj/status/1384742308591988736" target="_blank">Others have considered</a> reverse engineering the extension to figure out why it was so unnecessarily resource intensive, only to realise doing so could potentially be a crime. <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:3">
      <p>There are definitely ways to get SSHFS working natively on Windows, but they’re a lot messier, and setting up WSL is definitely advisable. <a href="#fnref:3" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:6">
      <p>I personally much prefer this setup over both the VS Code Remote-SSH and SSHFS extensions just because it seamlessly interfaces with WSL, which is my regular coding setup. <a href="#fnref:6" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:4">
      <p>Make sure to have the ‘Remote - WSL’ extension installed in VS Code. There’s also a weird WSL bug where the <code class="language-plaintext highlighter-rouge">code</code> command may not work if you’re in the <code class="language-plaintext highlighter-rouge">~/cse</code> directory, so you may need to run it from outside. <a href="#fnref:4" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:5">
      <p>It’s not the worst thing in the world if you forget/are unable to unmount, but you’ll be using up some resources both on your own computer as well as CSE computers. <a href="#fnref:5" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="unsw-cse" /><category term="guide" /><category term="ssh" /><summary type="html"><![CDATA[If you’re a VS Code user who is in first year, or isn’t too used to coding locally, you might be better off checking the easier method of using SSHFS with VS Code here.]]></summary></entry><entry><title type="html">CompClub CTF 2021</title><link href="/compclub-2021" rel="alternate" type="text/html" title="CompClub CTF 2021" /><published>2021-01-15T00:00:00+00:00</published><updated>2021-01-15T00:00:00+00:00</updated><id>/compclub-2021</id><content type="html" xml:base="/compclub-2021"><![CDATA[<p>I’ve been looking for the opportunity to do some form of high school outreach program for a while, so when CSESoc’s (my uni’s constituent society for computer science related degrees) CompClub decided to take applications for workshop developers, and saw the option for a security workshop, I knew I had to sign up. The following CTF was the result of my work with the help of a few fellow fantastic volunteers.</p>

<h3 id="la-table-des-matières">La table des matières</h3>
<ul>
  <li><a href="#behind-the-scenes" class="page-anchor-offset">Behind the scenes</a></li>
  <li><a href="#challenge-constraints" class="page-anchor-offset">Challenge constraints</a></li>
  <li><a href="#challenges-and-solutions" class="page-anchor-offset">Challenges and solutions!</a>
    <ul>
      <li>Crypto
        <ul>
          <li><a href="#a-strange-shift-30-points" class="page-anchor-offset">A strange shift</a></li>
          <li><a href="#compclub-30-points" class="page-anchor-offset">compclub</a></li>
          <li><a href="#shooting-stars-35-points" class="page-anchor-offset">Shooting Stars</a></li>
          <li><a href="#an-actual-flag-50-points" class="page-anchor-offset">An actual flag</a></li>
        </ul>
      </li>
      <li>OSINT
        <ul>
          <li><a href="#firednd-0-25-points" class="page-anchor-offset">firednd-0</a></li>
          <li><a href="#firednd-1-30-points" class="page-anchor-offset">firednd-1</a></li>
          <li><a href="#firednd-2-30-points" class="page-anchor-offset">firednd-2</a></li>
        </ul>
      </li>
      <li>Web
        <ul>
          <li><a href="#simplelogin-30-points-each" class="page-anchor-offset">simple-login</a></li>
        </ul>
      </li>
      <li>Forensics
        <ul>
          <li><a href="#spotify-20-points" class="page-anchor-offset">Spotify</a></li>
          <li><a href="#blank-space-30-points" class="page-anchor-offset">Blank Space</a></li>
          <li><a href="#egg-nog-50-points" class="page-anchor-offset">Egg nog</a></li>
        </ul>
      </li>
      <li>Miscellaneous
        <ul>
          <li><a href="#slides-10-points" class="page-anchor-offset">Slides</a></li>
          <li><a href="#what-was-that-25-points" class="page-anchor-offset">What was that?</a></li>
          <li><a href="#starlang-30-points" class="page-anchor-offset">starlang</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><a href="#conclusion" class="page-anchor-offset">Conclusion</a></li>
</ul>

<h1 id="behind-the-scenes">Behind the scenes</h1>
<p>While I’ve had the opportunity to write CTF challenges for <a href="https://unswsecurity.com" target="_blank">UNSW Security Society</a> over the past year, and hope to write plenty more this year, this was the first time when I’ve been responsible for getting the CTF infrastructure up and running, and deploying challenges.</p>

<p>If you’re interested in how I did this, particularly if you’re a CompClub workshop developer from the future and trying to figure out how to run a CTF, the README I wrote up in <a href="https://github.com/abiramen/2021-compclub-summer-ctf" target="_blank">this repo</a> should help you out.</p>

<p>The source code for all the challenges, as well as their writeups can be found there too!</p>

<h1 id="constraints">Constraints</h1>
<p>The CTFs I’ve written for in the past have ran for at least 4 hours, and at most 2 days. Writing a CTF with essentially a 2 hour time constraint was one of my main challenges. Another was catering to a variety of experience levels while remaining within the time limit. I also had to avoid OSINT challenges which involved trawling social media, since I couldn’t make the assumption that all students had access to social media.</p>

<h1 id="challenges-and-solutions">Challenges and solutions</h1>
<p>Here are all the challenges! The solutions are hidden until you click on them, so that you can try challenges without spoilers.</p>

<h2 id="crypto">Crypto</h2>
<h3 id="a-strange-shift-30-points">A strange shift (30 points)</h3>

<p>Worked two different shifts in one day at Maccas, and I’m too tired to decode this. This dude kept ordering a Chicken Caesar Salad, and uttering this strange code. Can you decode it for me?</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SQNL{sby_n_ibucqrw_xunsy}
</code></pre></div></div>

<h4 id="solution">Solution</h4>

<details>

  <h5 id="walkthrough">Walkthrough</h5>
  <p>The most obvious hint here is the reference to the Caesar cipher, further confirmed by the use of the word shift. However, we could try all 26 possibilities here, and none of them would result in a coherent FLAG.</p>

  <p>The sneakier part that you may not have caught in the wording is the ‘double shift’ - two different Caesar shifts were used.</p>

  <p>One thing we can note is that we can expect the message to start with FLAG. If we try to rig a shift so that we get the first letter to be F (+13), we get:</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FDAY{fol_a_vohpdej_khafl}
</code></pre></div>  </div>

  <p>Interestingly, this also gets the A in the right place, giving us <code class="language-plaintext highlighter-rouge">F_A_</code>.</p>

  <p>Shifting to get L in the second position (-5) gives us</p>
  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NLIG{nwt_i_dwpxlmr_spint}
</code></pre></div>  </div>

  <p>This time, we also get the G in the right place! This suggests that there are two alternating shifts.</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FDAY{fol_a_vohpdej_khafl}
NLIG{nwt_i_dwpxlmr_spint}
</code></pre></div>  </div>

  <p>Taking alternating letters, we get our flag.</p>

  <h5 id="flag">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{not_a_doppler_shift}</code></p>
</details>

<h3 id="compclub-30-points">compclub (30 points)</h3>

<p>compclub</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>compclubcompcompcompclubclubcomp compclubcompcompclubclubcompcomp
compclubcompcompcompcompcompclub compclubcompcompcompclubclubclub
compclubclubclubclubcompclubclub compclubclubcompcompcompclubcomp
compclubclubclubclubcompcompclub compclubcompclubcompclubcompcomp
compcompclubclubcompcompclubclub compclubcompclubclubclubclubclub
compclubclubclubcompcompclubclub compcompclubclubcompcompcompclub
compclubclubclubclubcompclubcomp compclubcompcompcompclubcompclub
compclubclubcompcompclubcompcomp compclubclubclubclubclubcompclub
</code></pre></div></div>

<h4 id="solution-1">Solution</h4>

<details>

  <h5 id="walkthrough-1">Walkthrough</h5>

  <p>We seem to have the strings ‘comp’ and ‘club’ as base units here - this seems very much like a binary system!</p>

  <p>There also seems to be exactly 8 comps or clubs in a group, or exactly a byte! This suggests that the FLAG could be encoded in ASCII.</p>

  <p>We know that all ASCII characters represented as a binary byte start with 0. Since all the groups start with comp, we can guess that comp is 0, and club is 1.</p>

  <p>Replacing, we get,</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>01001100 01000001 01000111 01111011 01100010 01111001 01010100 00110011 01011111 01110011 00110001 01111010 01000101 01100100 01111101
</code></pre></div>  </div>

  <p>Converting these into their ASCII characters, we get our flag.</p>

  <h5 id="flag-1">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{byT3_s1zEd}</code></p>
</details>

<h3 id="shooting-stars-35-points">Shooting Stars (35 points)</h3>

<p>found this cool book about shooting stars! i love shooting stars. the index fell out though.</p>

<p>Remember to wrap your answer with <code class="language-plaintext highlighter-rouge">FLAG{}</code>!</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/crypto/shooting-stars/_ctfd/files/book.txt">book.txt</a>
<a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/crypto/shooting-stars/_ctfd/files/index.txt">index.txt</a></p>

<h4 id="solution-2">Solution</h4>
<details>

  <h5 id="walkthrough-2">Walkthrough</h5>
  <p>The name of the file <code class="language-plaintext highlighter-rouge">index.txt</code> hints that the numbers in its contents could be some way that we could index the book, just like we index lists/arrays when programming.</p>

  <p>We can see the numbers occur in pairs, <code class="language-plaintext highlighter-rouge">(a, b)</code>. The <code class="language-plaintext highlighter-rouge">a</code> values can get pretty big, while <code class="language-plaintext highlighter-rouge">b</code> is always relatively small (at most, 6). This suggests that the second number could be some way of indexing a character within a word. Maybe the first number could be the index of the word we’re using?</p>

  <p>We can also see a couple of 0s occurring in the index, for example, the second pair is <code class="language-plaintext highlighter-rouge">(0, 0)</code>. This suggests that both words and characters are zero-indexed, just like lists and arrays in most programming languages start counting from 0.</p>

  <p>This is what is known as a book cipher. You could either try manually calculating each of the letters, writing a script to do it, or using a website <a href="https://www.boxentriq.com/code-breaking/book-cipher">like this one</a> to decode it. You’ll need to convert all the index pairs to match the format on that site. Make sure you select the correct options (word number, character number, none), and set the numbering start to 0. Once you do this, you should get your flag.</p>

  <h5 id="flag-2">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{lightpollution}</code></p>

  <h5 id="other-notes">Other notes</h5>
  <p>The contents of the book are actually the lyrics to Shooting Stars by Bag Raiders.</p>

</details>

<h3 id="an-actual-flag-50-points">An actual flag (50 points)</h3>

<p>Was bored, decided to colonise a new planet. Wanted to incorporate the planet name in its flag design - can you find it?</p>

<p>Remember to wrap your answer with <code class="language-plaintext highlighter-rouge">FLAG{}</code>!</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/crypto/an-actual-flag/_ctfd/files/anactualflag.png" target="_blank">anactualflag.png</a></p>

<h4 id="solution-3">Solution</h4>

<details>
  <h5 id="walkthrough-3">Walkthrough</h5>

  <p>Typically with an image challenge, I would go through</p>
  <ul>
    <li>Metadata (basically non-existent with PNG files)</li>
    <li>Photoshop levels (and other adjustments)</li>
    <li>Steganography</li>
    <li>Hexdump (checking through the raw bytes in the file for any anomalies, but not typical in a beginner’s CTF of this level).</li>
  </ul>

  <p>It’s certainly worth checking through these if everything else fails, but this is a crypto challenge, not a forensics challenge. The wording of the challenge also suggests that the name being hidden in the flag is intrisic to the flag design itself, and not the file.</p>

  <p>At this point, we can analyse the colours in the image - there are three of them:</p>

  <ul>
    <li>#486163</li>
    <li>#6b746f</li>
    <li>#706961</li>
  </ul>

  <p>It’s worth noting this image looks really dull. Breaking each of the colours into their subpixels (or bytes):</p>

  <ul>
    <li>48 61 63</li>
    <li>6b 74 6f</li>
    <li>70 69 61</li>
  </ul>

  <p>We can see that none of the bytes are greater than 7F, which is equal to 127 in decimal. This suggests that the colours may be being used to encode ASCII values.</p>

  <p>If we convert each byte to their corresponding ASCII values (remember to use the hexadecimal column on the ASCII table), we get</p>

  <ul>
    <li>H a c</li>
    <li>k t o</li>
    <li>p i a</li>
  </ul>

  <p>revealing the flag.</p>

  <h5 id="flag-3">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{Hacktopia}</code></p>

</details>

<h2 id="osint">OSINT</h2>
<h3 id="firednd-0-25-points">firednd-0 (25 points)</h3>

<p>Hello! CEO of FireDnD here! We’re a small startup that aims to connect people together for games of Dungeons and Dragons. You can check out our (incomplete - more on that later) website <a href="https://firednd-syd.web.app" target="_blank">here!</a></p>

<p>If this website is down, <a href="https://github.com/abiramen/2021-compclub-summer-ctf/tree/main/osint/firednd-src" target="_blank">you can find the source code here.</a></p>

<p>I’ve got a few tasks for you to complete, but before we start, could you do me a favour?</p>

<p>I’m supposed to meet up with someone after work today. I finish at 5PM, and take 8 minutes to walk from my office to Platform 2 of the nearest<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> station. I then need to catch the next T4 train, before I get off at the second stop. Can you figure out what time I get off the train (rounded to the nearest minute)?</p>

<p>Your answer should be a 24-hour time. For example, if your answer is 6:04PM, enter <code class="language-plaintext highlighter-rouge">FLAG{1804}</code>.</p>

<h4 id="solution-4">Solution</h4>

<details>
  <h5 id="walkthrough-4">Walkthrough</h5>
  <p>This requires some minor intelligence gathering:</p>
  <ol>
    <li>Note that FireDnD’s offices are located at 33-39 Hunter Street, Sydney. This can be found on the site’s footer, or ‘Contact Us’ page.</li>
    <li>The nearest station can be found using the measure distance tool on Google Maps - this is Martin Place station - however, it only barely beats out Wynyard station.</li>
    <li>We are told that the CEO wishes to take a T4 train from Platform 2 - if you chose Wynyard Station earlier, this is a big clue that something may have gone wrong, as <strong>no T4 trains pass through Wynyard</strong>. Avid train nerds may also note that Wynyard doesn’t have a Platform 2, as platforms there are numbered from 3. T4 trains on Platform 2 of Martin Place station run eastbound towards Bondi Junction. Two stops in this direction takes us to Edgecliff station.</li>
    <li>We now want to check the train schedule for T4 trains departing after 5:08pm on Fridays. The next train would leave at 5:12, and is scheduled to arrive at 5:16:30pm, making the answer 5:17pm, or 17:17.</li>
  </ol>

  <h5 id="flag-4">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{1717}</code></p>
</details>

<h3 id="firednd-1-30-points">firednd-1 (30 points)</h3>

<p>CEO of FireDnD here again!</p>

<p>Quite recently, I had to fire my frontend web developer, because they were doing a terrible job, as you can probably tell from our <a href="https://firednd-syd.web.app" target="_blank">incomplete website</a>. I’m still in the process of removing all traces of him from our current website. Your first task is to find a picture of our fired frontend team member, as I haven’t deleted it from our website’s resources yet, but I can’t find it for some reason.</p>

<p>Good luck!</p>

<h4 id="solution-5">Solution</h4>

<details>

  <h5 id="walkthrough-5">Walkthrough</h5>

  <p>Typically when I’m solving a CTF challenge which involves trying to find some hidden resource on a website, I’ll open the ‘Network’ tab of my browser, and start recording the requests the browser is making. These requests can tell me, for example, what images the browser is loading in - a fair initial suspicion would be that the browser is loading this image of the fired team member, but not displaying it.</p>

  <p>However, this isn’t quite the case. If we visit the /team.html page, we can see that three images are being loaded in: team-1.jpg, team-2.jpg and team-4.jpg. The glaring omission of team-3.jpg suggests that this might be our missing file. Visiting /team-3.jpg gives us our flag!</p>

  <h5 id="flag-5">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{h3Lps_t0_c0uNt}</code></p>
</details>

<h3 id="firednd-2-30-points">firednd-2 (30 points)</h3>

<p>No way, it’s me, the CEO of <a href="https://firednd-syd.web.app" target="_blank">FireDnD</a> again!</p>

<p>My fired frontend developer hid a secret webpage on our website. I found out about this cool way to prevent search engines from finding the page, so I used that to prevent the page from showing up. I also replaced the contents of the page. Surely no-one can find it now, right?</p>

<h4 id="solution-6">Solution</h4>

<details>

  <h5 id="walkthrough-6">Walkthrough</h5>

  <p>The first big hint in this challenge comes from the ‘cool way to prevent search engines from finding’ part - this is the exact function of robots.txt. If we visit /robots.txt, we find the following:</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User-agent: Googlebot
Disallow: /ry4ns-s3cret-p4ge.html
</code></pre></div>  </div>

  <p>We’ve now got the URL for the secret page! If we pay it a visit, however, we get this:</p>

  <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hi! CEO of FireDnD here! I have taken over Ryan's secret page after firing him. That being said, I heard that nothing is deleted from the internet forever. Surely no one has a copy of the old contents of this page?
</code></pre></div>  </div>

  <p>Unfortunate. Except, not really, because every internet historian’s favourite tool, The Internet Wayback Machine at <a href="web.archive.org" target="_blank">web.archive.org</a> happens to conveniently have a copy of the page!</p>

  <p>There’s an unloaded image on this page. Looking at the source code, we see</p>

  <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;div</span> <span class="na">class=</span><span class="s">"container"</span><span class="nt">&gt;</span>
    <span class="nt">&lt;h1&gt;</span>Ryan's top secret page<span class="nt">&lt;/h1&gt;</span>

    <span class="nt">&lt;p&gt;</span>Hello, I'm Ryan, a developer at firednd. This is my secret page!<span class="nt">&lt;/p&gt;</span>

    <span class="nt">&lt;p&gt;</span>Here is a secret:<span class="nt">&lt;/p&gt;</span>
    <span class="nt">&lt;img</span> <span class="na">src=</span><span class="s">"/web/20210106050020im_/http://firednd-syd.web.app/FLAG{hiDD3n_in_the_s0uRce}"</span><span class="nt">/&gt;</span>
<span class="nt">&lt;/div&gt;</span>
</code></pre></div>  </div>

  <p>giving us our flag.</p>

  <h5 id="flag-6">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{hiDD3n_in_the_s0uRce}</code></p>

</details>

<h2 id="web">Web</h2>

<h3 id="simplelogin-30-points-each">simplelogin (30 points each)</h3>

<p>When I grow up, I want to be a software developer at NASA. I’m still on my way to getting there. I decided to practice my web development skills and make an unbreakable login site. I bet you can’t hack me - I’ll even tell you that my username is <code class="language-plaintext highlighter-rouge">astronaut</code>.</p>

<p>Good luck!</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/tree/main/web/simplelogin-src" target="_blank">Challenge source code</a></p>

<p>There are three flags that can be found at the above link.</p>

<p>Flag 0 has three parts to it - you’ll have to find each part (labelled 1/3, 2/3 and 3/3) and join them together to get the full flag!</p>

<h4 id="flag-0-solution">Flag 0 solution</h4>

<details>

  <h5 id="walkthrough-7">Walkthrough</h5>

  <p>(Replace <code class="language-plaintext highlighter-rouge">Ctrl</code> with <code class="language-plaintext highlighter-rouge">Cmd</code> on macOS.)</p>

  <p>This first (well, zeroth) flag requires you to view the page source - that is, the code used to render the webpage to you - to look for comments. The flag is split up across three comments in three different files:</p>

  <ul>
    <li>
      <p>index.html</p>

      <p>This HTML file contains the overall structure of the page, and can be easily found through <code class="language-plaintext highlighter-rouge">Ctrl+Shift+I</code> and checking the ‘Elements’ tab or <code class="language-plaintext highlighter-rouge">Ctrl+U</code>, to view the raw HTML (on Chrome, at least).</p>
    </li>
    <li>
      <p>hello.js</p>

      <p>JS files usually make a webpage interactive. You could’ve found this from the ‘Sources’ tab, or noticing the JS file was loaded in the HTML source code, or even noticing the origin of the sneaky message in the ‘Console’ tab.</p>
    </li>
    <li>
      <p>style.css</p>

      <p>CSS files make pages look pretty. You again could’ve found this from the ‘Sources’ tab, or noticing the CSS file was loaded in the HTML source code.</p>
    </li>
  </ul>

  <h5 id="flag-7">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{0_i_f0unD_tHre3_tYpEs_oF_cOmM3NtS}</code></p>

</details>

<h4 id="flag-1-solution">Flag 1 solution</h4>

<details>

  <h5 id="walkthrough-8">Walkthrough</h5>

  <p>(Replace <code class="language-plaintext highlighter-rouge">Ctrl</code> with <code class="language-plaintext highlighter-rouge">Cmd</code> on macOS.)</p>

  <p>CTF authors like to involve cookies in challenges somehow, and this includes me! Using <code class="language-plaintext highlighter-rouge">Ctrl+Shift+I</code> on Chrome and selecting the Application tab, and then ‘Cookies’, reveals all cookies set by the current site, including the flag!</p>

  <h5 id="flag-8">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{1_i_cHeCkeD_f0r_c0oki3S}</code></p>

</details>

<h4 id="flag-2-solution">Flag 2 solution</h4>

<details>

  <h5 id="walkthrough-9">Walkthrough</h5>

  <p>You probably tried to login before anything else, but this is in fact the final flag! Logging in with incorrect details gives us the following message:</p>

  <p>‘Password did not match expected encoded result: <code class="language-plaintext highlighter-rouge">aXJlQUxMeUxpa0VzUGFjZVNoaVBzMjA3JA==</code>’</p>

  <p>This looks a lot like base-64 encoding - the biggest giveaway is the occurrence of equal signs on the end (which has a 2/3 probability of happening with base-64). Googling for a base-64 decoder gives us plenty of results, and decoding it gets us the password to use with the username ‘astronaut’ - ‘ireALLyLikEsPaceShiPs207$’. Logging in will give you the flag.</p>

  <p>You can also decode base-64 on a Linux command line with <code class="language-plaintext highlighter-rouge">echo 'aXJlQUxMeUxpa0VzUGFjZVNoaVBzMjA3JA==' | base64 -d</code>.</p>

  <h5 id="flag-9">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{2_b4Se_64_iS_nOt_eNcrYpTi0n}</code></p>

</details>

<h2 id="forensics">Forensics</h2>

<h3 id="spotify-20-points">Spotify (20 points)</h3>

<p>i made my spotify look pretty do you like it</p>

<p>Your flag will be a number. Wrap the number with <code class="language-plaintext highlighter-rouge">FLAG{}</code> - for example, if the number is 42, use <code class="language-plaintext highlighter-rouge">FLAG{42}</code>.</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/forensics/spotify/_ctfd/files/spotify.jpg" target="_blank">spotify.jpg</a></p>

<h4 id="solution-7">Solution</h4>

<details>
  <h5 id="walkthrough-10">Walkthrough</h5>
  <p>I tried to hints for this one in plain sight - taking a look at the two recent search items shows you a song named ‘Multiply’ and another named ‘Resolution’ - resolution is a term to describe how many pixels tall and wide an image/video is.</p>

  <p>From multiplying the resolution of the image (887 x 606), we get our solution.</p>

  <h5 id="flag-10">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{537522}</code></p>

  <h3 id="other-notes-1">Other notes</h3>
  <p>I made my Spotify look pretty using <a href="https://github.com/khanhas/spicetify-cli" target="_blank">Spicetify</a> with <a href="https://github.com/morpheusthewhite/spicetify-themes/tree/master/Dribbblish" target="_blank">this theme</a>.</p>
</details>

<h3 id="blank-space-30-points">Blank Space (30 points)</h3>

<p>i love to listen to Taylor Swift while stargazing. here’s a picture I took, but my camera was broken</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/forensics/blank-space/_ctfd/files/blankspace.png" target="_blank">blankspace.png</a></p>

<h4 id="solution-8">Solution</h4>

<details>
  <h5 id="walkthrough-11">Walkthrough</h5>
  <p>In the workshop slides, I highly suggested getting either Photoshop or GIMP to use - however, this was entirely solvable with just Microsoft Paint.</p>

  <p>I set the background of this image to #ffffff - this is the hexadecimal code for the colour white. I then used the paintbrush to hide the flag to #fffffd - a colour that is extremely close to white, but not quite, making the writing seem basically invisible.</p>

  <p>When solving image challenges for CTFs like these, I typically use Photoshop:</p>

  <ol>
    <li>Open the Levels tool from Edit &gt; Adjustments &gt; Levels</li>
    <li>Mess around with the input level sliders, and then the output level sliders and see if something reveals itself.</li>
  </ol>

  <p>This would’ve worked perfectly well for this challenge. However, a simpler solution would be:</p>

  <ol>
    <li>Open the image in Microsoft Paint.</li>
    <li>Use the Fill bucket tool, and select a colour like black.</li>
    <li>Click somewhere close to the border of the image, ‘flooding the image’.</li>
  </ol>

  <p>Paint only fills neighbouring pixels with the exact same colour - this should’ve revealed the off-white writing.</p>

  <h5 id="flag-11">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{st4rBucKs_Lov3Rs}</code></p>

  <h3 id="other-notes-2">Other notes</h3>
  <p>Yes, this flag was a reference to the misheard ‘Starbucks Lovers’ lyrics from Taylor Swift’s Blank Space.</p>

</details>

<h3 id="egg-nog-50-points">Egg nog (50 points)</h3>

<p>It’s hard to find egg nog after Christmas. It’s also hard to find my hidden message, I hope!</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/forensics/egg-nog/_ctfd/files/eggnog.jpg" target="_blank">eggnog.jpg</a>
<a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/forensics/egg-nog/_ctfd/files/steggnog.png" target="_blank">steggnog.png</a></p>

<h4 id="solution-9">Solution</h4>

<details>

  <h5 id="walkthrough-12">Walkthrough</h5>
  <p>While this challenge uses a set of techniques that are relatively common in CTFs, paying attention to the workshop slides would have helped a lot here.</p>

  <p>The first image you want to look at is eggnog.jpg - note that the contents featured in both images are basically identical, although the PNG is of much lower resolution. Looking at the file’s metadata, we can see that there is some hidden information in the EXIF data (JPG files can have EXIF data, PNG files don’t) - namely, the following string:</p>

  <p><code class="language-plaintext highlighter-rouge">jocksfindquartzglyphvexbmw</code></p>

  <p>We shall save this for later use.</p>

  <p>The second image is named steggnog.png - a huge hint that more advanced image steganography, like least significant bit steganography, might be in use here. Googling ‘online steganography’ yields <a href="https://stylesuxx.github.io/steganography/" target="_blank">this website</a>. sDecoding it yields the following:</p>

  <p><code class="language-plaintext highlighter-rouge">FAJI{tzhpzdtpditdfdcjthtzx}</code></p>

  <p>This is the point where this challenge now becomes more crypto than forensics - we can see the result of our steganography decode looks almost like a flag. This is where we go back to our older string - <code class="language-plaintext highlighter-rouge">jocksfindquartzglyphvexbmw</code>. Googling this string yields a result about ‘perfect pangrams’ - sentences with all 26 letters of the alphabet exactly once. You might notice that this perfect pangram makes a perfect key for a <em>substitution cipher</em>.</p>

  <p>Decoding the substitution cipher (using the perfect pangram as our ciphertext alphabet) on a website like <a href="https://cryptii.com/pipes/alphabetical-substitution" target="_blank">this</a> gives us our flag.</p>

  <h5 id="flag-12">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{notsoinsignificantnow}</code></p>

  <h3 id="other-notes-3">Other notes</h3>
  <p>The reason the PNG was lower resolution was so that your computer didn’t die trying to decode using the steganography tool - fewer pixels means less work.</p>

</details>

<h2 id="miscellaneous">Miscellaneous</h2>

<h3 id="slides-10-points">Slides (10 points)</h3>

<p><a href="https://docs.google.com/presentation/d/1wS4-rn9sk3RTaAJHo2DJmBTq2G48ULC4ezQbbco7Gio/edit?usp=sharing" target="_blank">Here are the slides</a> from today’s workshop. It comes with a free flag!</p>

<h4 id="solution-10">Solution</h4>

<details>
  <h5 id="walkthrough-13">Walkthrough</h5>
  <p>Find the slide about what CTF flags are - it’s in plain sight!</p>

  <h5 id="flag-13">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{y0ur_f1rSt_flaG}</code></p>
</details>

<h3 id="what-was-that-25-points">What was that? (25 points)</h3>

<p>Houston, we’ve got… a disappearing black hole??</p>

<p>Make sure that the FLAG{} contents are all in lowercase.</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/misc/what-was-that/_ctfd/files/whatwasthat.mp4" target="_blank">whatwasthat.mp4</a></p>

<h4 id="solution-11">Solution</h4>

<details>
  <h5 id="walkthrough-14">Walkthrough</h5>
  <p>This challenge features the distinctive short and long flashes (or dots and dashes used in Morse code). Fortunately, there’s also synced beeping noises with the video, which makes interpreting the Morse code a lot easier (although you can just use the video, too!):</p>

  <ol>
    <li>Open the audio from the video in an audio editor like Audacity.</li>
    <li>Use the spectrogram to identify where each short (dot) flash or long (dash) sound is, and enter it into a Morse Code translator, and voila!</li>
  </ol>

  <h5 id="flag-14">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{no_remorse}</code></p>
</details>

<h3 id="starlang-30-points">starlang (30 points)</h3>

<p>I made my own programming language called starlang! It’s pretty self-explanatory. Can you figure out what this program prints?</p>

<p>(Make sure to open the file with a text editor so that you can read its contents.)</p>

<p><a href="https://github.com/abiramen/2021-compclub-summer-ctf/blob/main/misc/starlang/_ctfd/files/my_program.starlang" target="_blank">my_program.starlang</a></p>

<h4 id="solution-12">Solution</h4>

<details>

  <h5 id="walkthrough-15">Walkthrough</h5>
  <p>This challenge uses a fake, but straight-forward ‘programming language’ - this is what we call ‘pseudocode’, which is a simplified notation to describe how a program or algorithm should work.</p>

  <p>There are some things that you might need to be careful for here:</p>

  <ul>
    <li>If a ‘branch’ of an if statement is true, then we don’t need to worry about other branches of the if statement. For example:</li>
  </ul>

  <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if </span><span class="p">(</span><span class="nx">a</span><span class="p">)</span> <span class="p">{</span>
    <span class="nf">do_something</span><span class="p">();</span>
<span class="p">}</span> <span class="k">else</span> <span class="k">if </span><span class="p">(</span><span class="nx">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="nf">do_something_else</span><span class="p">();</span>
<span class="p">}</span> <span class="k">else</span> <span class="k">if </span><span class="p">(</span><span class="nx">c</span><span class="p">)</span> <span class="p">{</span>
    <span class="nf">do_other_thing</span><span class="p">();</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="nf">do_last_thing</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div>  </div>

  <p>If <code class="language-plaintext highlighter-rouge">b</code> is true, then it doesn’t matter if <code class="language-plaintext highlighter-rouge">c</code> is true as well - once we’ve completed <code class="language-plaintext highlighter-rouge">do_something_else()</code>, we skip to the end of the entire if statement structure.</p>

  <ul>
    <li>Be careful with ‘less than’ - this is not the same as ‘less than or equal to’.</li>
  </ul>

  <p>Following through each of the statements should get you to the flag.</p>

  <h5 id="flag-15">Flag</h5>
  <p><code class="language-plaintext highlighter-rouge">FLAG{n3buL0uS_r3d5h1fT}</code></p>
</details>

<h1 id="conclusion">Conclusion</h1>

<p>On the day, we received 928 attempted flag submissions amongst 54 competitors in 10 teams, of which, 127 of submissions were correct.</p>

<p>Thanks to all the students that came along and participated in workshops and the CTF! I hope to see you all next year, when we can hopefully actually run this event in person.</p>

<p>Also a shoutout to all the CompClub team, workshop developers and mentors for such a fantastic week, as well as all the random people I’ve asked to test my challenges over the past 2 weeks!</p>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>Nearest by straight-line distance. The location of a station can be determined by the position of the ‘T’ logo on Google Maps. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="security" /><category term="ctf" /><summary type="html"><![CDATA[I’ve been looking for the opportunity to do some form of high school outreach program for a while, so when CSESoc’s (my uni’s constituent society for computer science related degrees) CompClub decided to take applications for workshop developers, and saw the option for a security workshop, I knew I had to sign up. The following CTF was the result of my work with the help of a few fellow fantastic volunteers.]]></summary></entry><entry><title type="html">Making this site</title><link href="/making-site" rel="alternate" type="text/html" title="Making this site" /><published>2020-09-19T00:00:00+00:00</published><updated>2020-09-19T00:00:00+00:00</updated><id>/making-this-site</id><content type="html" xml:base="/making-site"><![CDATA[<p>Hi! If you’re here, that means I’ve finally got around to fully completing my website! I purchased this domain 10
months ago, mostly neglected it for 9 of those months, or was super indecisive about how I wanted to approach it, and
then finally got around to working on it.</p>

<h2 id="planning">Planning</h2>
<p>I knew the main feature I wanted to implement was a blog, and that I also wanted some place for a portfolio, while having a relatively unique design compared to most blogs. With this in mind, I quickly threw something together in Adobe Illustrator.</p>
<h2 id="making-a-logo">Making a logo</h2>
<p>I’ve had an idea in my mind to have some kind of ramen logo out of neon lights. After creating a wireframe in Illustrator, and a few Photoshop tutorials on neon glow effects later, I’ve got a new logo to use everywhere! :)</p>
<h2 id="a-tech-stack">A tech stack</h2>
<p>This was probably the part that held me off from working on this for the longest. For a long time, I was convinced that I would build my site using Vue.js, with the Vuetify component library, due to my prior experience with Vue. I also contemplated throwing in Nuxt in order to be able to server-side render some things. At some point, I realised that this was all extremely overkill, and would require actively running a server, which had many of its own costs.</p>
<h3 id="github-pages-and-dr-jekyll">GitHub Pages, and Dr Jekyll</h3>
<p>Once I realised that a static site would better suit my needs, I knew that I would use GitHub Pages for hosting. A friend of mine, ralismark, had mentioned how great Jekyll was, using it for <a href="https://www.ralismark.xyz" target="_blank">their own site</a>. I had known that Jekyll was a thing for a while, ever since I created my first GitHub Pages site in 2014, but had always ignored it, naively thinking that pure HTML/CSS was the way to go at the time.</p>

<p>After building this site with Jekyll, I have to say it’s an absolute delight to work with. GitHub automatically builds a Jekyll site on push, and deploys it. Jekyll provides amazing templating support, reducing redundancy.Another fantastic feature is built-in support for writing pages and posts in Markdown (this is my first post in Markdown!! Hello world!!), for insertion into templates. While there are many Markdown-to-HTML converters out there, Jekyll just makes it seamless.</p>

<p>If I ever require a page with server-side rendering, I can always host that on a separate domain. ralismark still says that Jekyll is great, although she does wish that he could server-side render LaTeX so that she doesn’t need to do that on the client’s device.</p>
<h3 id="frontend-frameworks">Frontend frameworks</h3>
<p><del>Admittedly as a result of me still being stuck in my old ways (or just being lazy), this site still uses jQuery in few places. I intend on porting it all to vanilla JavaScript at some point.</del> This site is now free of jQuery!  I also made the mistake of deciding to use Bootstrap when I started writing this site - mainly for layout (responsive containers), but you can probably see, this site looks relatively un-Bootstrapish. I also intend on removing Bootstrap at some point, since most of the layout stuff shouldn’t be too hard to reimplement myself.</p>
<h2 id="fun-stuff">Fun stuff</h2>
<p>One of <a href="https://mango.pdf.zone" target="_blank">my favourite blogs</a>, by the legendary <a href="https://twitter.com/mangopdf">@mangopdf</a>, has a couple of easter eggs baked into it. I decided to take some inspiration and add some fun stuff too! At the moment, there’s only one easter egg, which you’ll have to head to the homepage to learn about, but there’s definitely more coming soon!</p>

<p>I also eventually realised that mangopdf’s blog uses Jekyll too, so I knew that I had made a good choice. 😎</p>

<h2 id="the-future">The future</h2>
<p>Well, I have quite a few posts planned, so watch this space I guess! 🙃</p>]]></content><author><name></name></author><category term="meta" /><summary type="html"><![CDATA[Hi! If you’re here, that means I’ve finally got around to fully completing my website! I purchased this domain 10 months ago, mostly neglected it for 9 of those months, or was super indecisive about how I wanted to approach it, and then finally got around to working on it.]]></summary></entry><entry><title type="html">CSE and VS Code (legacy)</title><link href="/cse-setup-legacy" rel="alternate" type="text/html" title="CSE and VS Code (legacy)" /><published>2020-09-18T00:00:00+00:00</published><updated>2020-09-18T00:00:00+00:00</updated><id>/cse-setup-old</id><content type="html" xml:base="/cse-setup-legacy"><![CDATA[<p class="alert alert-red"><strong>WARNING</strong> This method is now considered unreliable due to the load it places on CSE servers, and may be blocked by CSE’s System Support at any time. Check out <a href="/cse-setup">this newer post</a> instead. Do not use this on CSE.</p>

<p>Hi there! If you’re new here, welcome to CSE! We’re going to be setting up a text editor called Visual Studio Code, or VS Code, to remotely work on the CSE computers without needing a VNC client!</p>

<h2 id="why-are-we-doing-this">Why are we doing this?</h2>

<p>You’ve likely used a program called TigerVNC to connect to the School of CSE’s computers before, which provides you a visual interface with a full desktop environment, very similar to the experience you would get out of sitting in front of a physical computer on campus.</p>

<p>If you’re currently doing COMP1511, I would strongly advise you to use the provided desktop environment and gedit over TigerVNC for the first week, just to familiarise yourself with it. Don’t take your experience with it as a sign that Linux has to be ugly either - <a href="https://reddit.com/r/unixporn" target="_blank">this subreddit has plenty of proof otherwise</a>. (Despite the name, I promise it’s safe for work.)</p>

<p>More importantly, under normal circumstances, you would be doing your exams in front of a similarly configured physical computer on campus. As of the start of 2021 however, it seems you’re generally free to use whatever setup you wish.</p>

<p>The solution we’re about to go through provides a more seamless coding experience without having to load a full visual interface from CSE computers. This has the added benefits of not being affected by compression, especially if you have a poor connection, and other latency issues.</p>

<div class="alert">
  <p>I don’t particularly recommend this for first years, but if you’re a more experienced user with your own local setup, you might want to checkout this more lightweight method <a href="/cse-sshfs">here</a>.</p>
</div>

<h2 id="getting-started">Getting started</h2>

<p>First of all, we’re going to acquaint ourselves with Visual Studio Code, or VS Code. You’ve likely been introduced to a tool called gedit which allows you to write files of text (including code), or what’s called a text editor. VS Code does th is, but it provides many more great features on top. To get started, you’ll need to install VS Code on your own computer, whether it be Windows, macOS or Linux.</p>

<ol>
  <li><strong>Download and install Visual Studio Code.</strong> You can find a download <a href="https://code.visualstudio.com/download" target="_blank">here</a>.</li>
  <li><strong>If prompted to do so during the installation process</strong>, select the checkbox which says ‘Add to PATH’.</li>
  <li><strong>Restart your computer once you’re done.</strong></li>
</ol>

<p>You should now have VS Code installed! You can now edit files on your own computer, but that’s not quite what we want - our goal is to edit files on CSE machines.</p>

<h2 id="our-friend-ssh">Our friend, SSH</h2>
<p>Some cool computer scientists invented a way (or more specifically, a set of rules, or protocols) to allow you to communicate with remote computers over the internet, called SSH, or Secure Shell. This allows us to edit files on another computer, such as the CSE servers, and run commands through a terminal on CSE servers.</p>

<p>Luckily for us, VS Code has a way to use SSH to allow you to edit files and run commands on other computers.</p>

<div class="alert">
  <p>If you’re on macOS, use <kbd>Cmd</kbd> instead of <kbd>Ctrl</kbd>.</p>
</div>

<h3 id="part-1-getting-started">Part 1: Getting started</h3>
<ol>
  <li><strong>Open VS Code.</strong></li>
  <li><strong>Open the Extensions menu.</strong> You can access it using <code class="language-plaintext highlighter-rouge">Ctrl+Shift+X</code>.</li>
  <li><strong>Search for and install the Remote-SSH extension.</strong> Once this is done, you might be prompted to reload the window.
 <img src="/assets/images/blog/cse-setup-old/ssh-extension.jpg" alt="Install the SSH extension." /></li>
</ol>

<h3 id="part-2-improving-security">Part 2: Improving security</h3>

<div class="alert">
  <p>Not performing this step can make your CSE account accessible to anyone while you’re connected. Accounts that don’t enable this will not be able to use VS Code to connect to CSE computers in the future.</p>
</div>

<ol>
  <li><strong>Open the VS Code settings pane.</strong> To do this, click the settings icon near the bottom left, or use <code class="language-plaintext highlighter-rouge">Ctrl+,</code>.</li>
  <li><strong>Search for ‘Remote Server Listen on Socket’.</strong> Tick the corresponding checkbox once it appears.
<img src="/assets/images/blog/cse-setup-old/socket.jpg" alt="Enable the Remote Server Listen on Socket option" /></li>
  <li><strong>Search for ‘Lockfiles In Tmp’.</strong> Again, select the corresponding checkbox.
<img src="/assets/images/blog/cse-setup-old/lockfiles.jpg" alt="Lockfiles in Tmp" /></li>
  <li>Close and reopen VS Code.</li>
</ol>

<p>Congrats! You’re now safer from nefarious hackers, or inadvertent intruders.</p>

<p>Note: If you’re coming here from the older version of my guide, please switch to using <strong>vscode.cse.unsw.edu.au</strong>, rather than login, wagner, weill or williams. If not you can ignore this.</p>

<h3 id="part-3-logging-in">Part 3: Logging in</h3>
<ol>
  <li>
    <p><strong>Open the Remote-SSH menu.</strong> This can be done by clicking this button</p>

    <p><img src="/assets/images/blog/cse-setup-old/ssh-icon.jpg" alt="SSH Icon" /> 
in the bottom left. The menu should look like this:</p>

    <p><img src="/assets/images/blog/cse-setup-old/ssh-menu.jpg" alt="SSH menu" /></p>
  </li>
  <li><strong>Click Connect to Host.</strong> In the field that shows up, type in <code class="language-plaintext highlighter-rouge">z5555555@vscode.cse.unsw.edu.au</code>, using your zID instead. Hit <kbd>Enter</kbd>.
 A new window will open.</li>
  <li><strong>If asked to select a remote platform</strong>, select Linux.</li>
  <li><strong>Enter your password when prompted.</strong> If your password is wrong, you will prompted to retry. Once you’ve done that, you should be connected successfully!
    <ul>
      <li>Use <kbd>Ctrl+O</kbd> to open a file.</li>
      <li>Use <kbd>Ctrl+`</kbd> (that’s the key to the left of the number row) to open a terminal pane.
This is exactly like a terminal window in TigerVNC. You can run <code>ls</code> to see your files!</li>
    </ul>
  </li>
  <li><strong>We can save some time by adding this to our hosts list.</strong> Click the SSH icon in the bottom left again, click Connect to Host, and this time, click <strong>Add New SSH Host.</strong></li>
  <li><strong>Enter the following command:</strong> <code class="language-plaintext highlighter-rouge">ssh z5555555@vscode.cse.unsw.edu.au</code>, replacing with your own zID again, and hit <kbd>Enter</kbd>. If prompted which configuration file to save to, select the first one.</li>
  <li><strong>You should now see vscode.cse.unsw.edu.au on the Connect to Host menu.</strong> This means you don’t have to type it in each time.
 <img src="/assets/images/blog/cse-setup-old/ssh-menu-2.jpg" alt="SSH menu" />
If you had an issue, scroll down to <a href="#troubleshooting" class="page-anchor-offset">troubleshooting</a> section.</li>
</ol>

<h3 id="part-4-vscode2---electric-boogaloo">Part 4: vscode2 - Electric Boogaloo</h3>

<p>vscode.cse.unsw.edu.au connects you to a CSE server called <code class="language-plaintext highlighter-rouge">corelli</code>, which works fine most of the time. However, the server software VS Code uses in order to form a connection is quite resource intensive, and can often eat up all the resources that <code class="language-plaintext highlighter-rouge">corelli</code> has available.</p>

<p>Thankfully, CSE has set up a second server called <code class="language-plaintext highlighter-rouge">mandolin</code> you can use in case you’re having issues with corelli.</p>

<p>To set up your VS Code to be able to connect to <code class="language-plaintext highlighter-rouge">mandolin</code>, repeat Part 3, but replace all occurrences of <code class="language-plaintext highlighter-rouge">z5555555@vscode.cse.unsw.edu.au</code> with <code class="language-plaintext highlighter-rouge">z5555555@vscode2.cse.unsw.edu.au</code>. That’s right, you just need to stick a <code class="language-plaintext highlighter-rouge">2</code> after <code class="language-plaintext highlighter-rouge">vscode</code>!</p>

<p>You can connect to <code class="language-plaintext highlighter-rouge">mandolin</code> if <code class="language-plaintext highlighter-rouge">corelli</code> ever feels slow, which it might during assignment season.</p>

<h2 id="making-vs-code-even-cooler">Making VS Code even cooler</h2>

<p><img src="/assets/images/blog/cse-setup-old/vscode.jpg" alt="My VS Code over SSH" /></p>

<h3 id="editor-rulers">Editor rulers</h3>
<p>The COMP1511 style guide requires you to make sure your lines of code aren’t longer than 80 characters (and it’s good practice to do so!). You can make VS Code show a handy little ruler to make sure your lines aren’t too long.</p>

<ol>
  <li><strong>Open the settings.json file.</strong> To do this, press <kbd>Ctrl+Shift+P</kbd> and search for <strong>Open Settings (JSON).</strong> A file should open.</li>
  <li><strong>We need to add to this file.</strong> You should see that the last line is a curly brace like this: <code>}</code>. What we want to do is add a comma to the second last line. Then, create a new line after the comma, and paste this:
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"[c]" :{
 "editor.rulers" : [
     80
 ]
}
</code></pre></div>    </div>
  </li>
</ol>

<h3 id="the-cc-extension">The C/C++ extension</h3>
<p>The VS Code C/C++ extension will make your life a lot easier when coding. To install it,</p>
<ol>
  <li><strong>Open a Remote-SSH window</strong> and connect to CSE servers.</li>
  <li><strong>Open the Extensions pane</strong> by using <code class="language-plaintext highlighter-rouge">Ctrl+Shift+X</code>.</li>
  <li>Search for C/C++ and install the extension by Microsoft.</li>
</ol>

<h3 id="other-extensions">Other extensions</h3>
<p>VS Code has heaps of themes and extensions. Some which I use:</p>
<ul>
  <li><strong>C/C++ Snippets.</strong> Occasionally saves time with suggestions. Make sure to install from your Remote-SSH window.</li>
  <li><strong>Material Icon Theme.</strong></li>
  <li><strong>Dainty and Dainty - Material Theme.</strong> I personally use Dainty - Material Theme Ocean.</li>
  <li><strong>Discord Rich Presence.</strong> If you use Discord, then it’ll update your currently playing status with details of what you’re coding on. If one of your friends tries to drag you into a voice call, you can just open up VS Code and tell them that you’re busy working :P</li>
</ul>

<h2 id="bonus-coins-ssh-and-ssh-keys">Bonus coins: SSH and SSH keys</h2>
<p>Congrats! You made it to the bonus section. Following these instructions will mean you won’t be asked for a password each time you open a new SSH window, while still keep your connection secure, using magic in the form of mathematics.</p>

<h4 id="part-1-generate-a-keypair">Part 1: Generate a keypair</h4>
<h5 id="windows-10">Windows 10</h5>

<ol>
  <li><strong>Open the Windows Command Prompt.</strong> You can do this by entering <code class="language-plaintext highlighter-rouge">cmd</code> in Search.</li>
  <li><strong>Generate a keypair.</strong>  Use the command <code class="language-plaintext highlighter-rouge">ssh-keygen -t rsa -b 4096</code>.</li>
  <li><strong>You’ll be asked where you want to save the key.</strong> Leave this blank, and press <kbd>Enter</kbd>.</li>
  <li><strong>You’ll be asked to set a password.</strong> Leave this blank, and press <kbd>Enter</kbd> again. Do the same when you are asked to confirm it.</li>
  <li><strong>Using the same Command Prompt as before</strong>, enter the command <code class="language-plaintext highlighter-rouge">code "%userprofile%/.ssh/id_rsa.pub"</code>. This should open up a file in VS Code, starting with ssh-rsa and followed by a long mess of characters. <sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup> Don’t close this file, but use <kbd>Ctrl+A Ctrl+C</kbd> to copy the entire contents of this file.</li>
</ol>

<h5 id="linuxmacos">Linux/macOS</h5>

<ol>
  <li><strong>Open the Terminal.</strong></li>
  <li><strong>Generate a keypair.</strong>  Use the command <code class="language-plaintext highlighter-rouge">ssh-keygen -t rsa -b 4096</code>.</li>
  <li><strong>You’ll be asked where you want to save the key.</strong> Leave this blank, and press <kbd>Enter</kbd>.</li>
  <li><strong>You’ll be asked to set a password.</strong> Leave this blank, and press <kbd>Enter</kbd> again. Do the same when you are asked to confirm it.</li>
</ol>

<p>Your computer has now generated two files: <code class="language-plaintext highlighter-rouge">id_rsa</code> and <code class="language-plaintext highlighter-rouge">id_rsa.pub</code>. The <code class="language-plaintext highlighter-rouge">id_rsa.pub</code> file is what’s called a public key, while <code class="language-plaintext highlighter-rouge">id_rsa</code> is a private key. <strong>Never share (or even open) your private key.</strong> It’s called a private key for a reason :P</p>

<h4 id="part-2-using-the-keypair-for-cse-machines">Part 2: Using the keypair for CSE machines</h4>
<p>Now, we need to tell the CSE computer that your computer is trustworthy, and you are who you claim to be. To do this, we need to give the CSE computer the public key you just generated. If you’re on Linux/macOS, this should be pretty easy, but if you’re on Windows 10, there’s a few more steps.</p>

<h5 id="windows-10-1">Windows 10</h5>

<ol>
  <li>
    <p><strong>Using your existing SSH or VLAB connection, open a terminal on the CSE computers.</strong> Run the following command. <sup id="fnref:3"><a href="#fn:3" class="footnote" rel="footnote" role="doc-noteref">2</a></sup></p>

    <p><code class="language-plaintext highlighter-rouge">mkdir ~/.ssh;chmod 700 ~/.ssh;touch ~/.ssh/authorized_keys;chmod 600 ~/.ssh/authorized_keys;code ~/.ssh/authorized_keys</code></p>
  </li>
  <li>
    <p><strong>A window for a file called authorized_keys should open in VS Code.</strong> Paste your public key from earlier into this file, and save.</p>
  </li>
</ol>

<h5 id="linuxmacos-1">Linux/macOS</h5>
<ol>
  <li><strong>Open the Terminal.</strong></li>
  <li>
    <p><strong>Run the following command</strong>, replacing with your own zID.</p>

    <p><code class="language-plaintext highlighter-rouge">ssh-copy-id z5555555@cse.unsw.edu.au</code></p>
  </li>
  <li><strong>Enter your password.</strong> You’ll be asked for your CSE password one last time, for the key to be copied over.</li>
</ol>

<p>Voila! You had it a bit easier here.</p>

<h4 id="part-3-telling-vs-code-on-your-computer-to-use-your-new-keys">Part 3: Telling VS Code on your computer to use your new keys</h4>

<ol>
  <li><strong>On your local machine, open the SSH menu in VS Code.</strong> Click the SSH icon in the bottom left, then <strong>Open Configuration File</strong>, and select the first option. You should see something like this:</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host vscode.cse.unsw.edu.au
    HostName vscode.cse.unsw.edu.au
    User z5555555

Host vscode2.cse.unsw.edu.au
    HostName vscode2.cse.unsw.edu.au
    User z5555555
</code></pre></div></div>

<ol>
  <li><strong>We need to tell the SSH connection to use our private key.</strong> Add the following line to your config file, after each of the lines with ‘User’ on it. Make sure to indent it so that it’s in line with the others.
    <ul>
      <li>
        <p><strong>Windows:</strong> Make sure to enter your username. If you don’t know it, it’s the part after <code class="language-plaintext highlighter-rouge">C:/Users/</code> when you open <code class="language-plaintext highlighter-rouge">cmd</code>.</p>

        <p><code class="language-plaintext highlighter-rouge">IdentityFile "C:/Users/[YOURUSERNAME]/.ssh/id_rsa"</code></p>
      </li>
      <li>
        <p><strong>Linux/macOS</strong></p>

        <p><code class="language-plaintext highlighter-rouge">IdentityFile ~/.ssh/id_rsa</code></p>
      </li>
    </ul>
  </li>
</ol>

<p>A potential completed config might look like this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host vscode.cse.unsw.edu.au
    HostName vscode.cse.unsw.edu.au
    User z5555555
    IdentityFile "C:/Users/Bob/.ssh/id_rsa

Host vscode2.cse.unsw.edu.au
    HostName vscode2.cse.unsw.edu.au
    User z5555555
    IdentityFile "C:/Users/Bob/.ssh/id_rsa
</code></pre></div></div>

<p>Once you’ve done this, you should be able to open Remote-SSH in VS Code without requiring a password.</p>

<p>If you need to add another public key to authorized_keys (for another computer), you can enter it on a new line.</p>

<h2 id="coding-on-your-own-computer">Coding on your own computer</h2>

<p>This guide was once part of a much larger guide which involved setting up to allow you to code on your own computer. However, this was much more than what most 1511 students need. It’s worth noting that you need to have an internet connection for SSH to work. If your train route has poor reception, and you want to code on the train, you would need an alternative.</p>

<p>Even then, I would still recommend setting up your own local coding setup at some point. If you’re new to coding and you’re doing 1511 right now, I wouldn’t say you should worry about this. However, if you do intend on going on to study more CSE courses, I recommend that you come back over the break after term ends and <a href="/wsl-github">check out this other post I wrote</a>, which goes through setting up a Linux development environment if you have a Windows 10 computer, and using Git and GitHub to keep copies of your work on different computers and the internet.</p>

<h2 id="troubleshooting-ssh-over-vs-code">Troubleshooting SSH over VS Code</h2>
<p>If you’re having trouble connecting to VS Code, a fix that tends to work is</p>

<ol>
  <li><strong>Log in to the CSE servers using TigerVNC.</strong></li>
  <li><strong>Open a terminal.</strong></li>
  <li><strong>Run the following command:</strong> <code class="language-plaintext highlighter-rouge">rm -r ~/.vscode-server</code>. Make sure to copy and paste this command carefully.</li>
</ol>

<p>Alternatively, if you know how to open a Command Prompt/Terminal, you can run <code class="language-plaintext highlighter-rouge">ssh -t z5555555@cse.unsw.edu.au "rm -r ~/.vscode-server"</code>, replacing with your own zID.</p>

<p>If this doesn’t work, try some of the solutions <a href="https://stackoverflow.com/questions/59978826/why-ssh-connection-timed-out-in-VS Code" target="_blank">here</a>.</p>

<h2 id="have-feedback-or-want-to-reach-out">Have feedback, or want to reach out?</h2>
<p>If you’ve spotted an error, or have any suggestions or improvements that I can make, feel free to contact me! My details are at the bottom of this page.</p>

<h2 id="official-guide">Official guide</h2>

<p>Despite myself and the people being acknowledged below currently being employed by the School of CSE, this blogpost doesn’t necessarily represent the views of my employer.</p>

<p>As of late October 2020, CSE now has an official guide available for using VS Code to connect over SSH. While the steps you’ve just followed cover all of what’s in the guide (and more!), and is up-to-date as of the time of writing, you can find the official guide <a href="https://www.cse.unsw.edu.au/~learn/homecomputing/vscode/" target="_blank">here</a>.</p>

<h2 id="acknowledgements">Acknowledgements</h2>

<p>Thanks to the following people who’ve looked over this guide and provided feedback!</p>

<ul>
  <li><a href="https://github.com/ralismark" target="_blank">ralismark</a></li>
  <li><a href="https://github.com/adaluong" target="_blank">adaluong</a></li>
  <li><a href="https://github.com/lukefisklennon" target="_blank">lukefisklennon</a></li>
  <li><a href="https://github.com/tfpk" target="_blank">tfpk</a></li>
</ul>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>This actually represents an extremely large number, which is used to form a mathematical lock. This is called RSA encryption and you can learn more about it in MATH3411. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:3">
      <p>Technically multiple commands, which I’ve condensed down for you. <a href="#fnref:3" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name></name></author><category term="unsw-cse" /><category term="guide" /><category term="vscode" /><summary type="html"><![CDATA[WARNING This method is now considered unreliable due to the load it places on CSE servers, and may be blocked by CSE’s System Support at any time. Check out this newer post instead. Do not use this on CSE.]]></summary></entry><entry><title type="html">Setting up WSL and GitHub (archive)</title><link href="/wsl-github" rel="alternate" type="text/html" title="Setting up WSL and GitHub (archive)" /><published>2020-09-18T00:00:00+00:00</published><updated>2020-09-18T00:00:00+00:00</updated><id>/wsl-and-github</id><content type="html" xml:base="/wsl-github"><![CDATA[<div class="alert alert-red">
    This post is no longer up to date. I generally now advise most people use WSL 2. I may or may not write an updated version of this post in the future.
</div>

<p>
    If you're a UNSW student, there's a good chance that you're here from my CSE setup guide for vscode.
    This is
    going to be a short guide to setting up WSL, which provides you with a Linux environment within Windows. To go
    through
    the WSL section, you'll need to be on Windows 10. If you're here just to learn some basic Git, you can
    <a href="#git">click here</a> to skip all the Windows stuff, free of charge!
</p>

<h2 id="wsl">Coding locally on your Windows 10 computer with WSL</h2>
<p>Windows 10 now supports running Linux distributions from within it. We can set this up so that we can easily
    use a lot of the Linux commands and scripting which is taught in UNSW CSE degrees. You can even use your new Linux
    setup to go on and learn other languages over breaks, such as <a href="https://doc.rust-lang.org/book/"
        target="_blank">Rust</a>.
    <br><br>
    Before we proceed, there's two versions of WSL that you can use - WSL1 and WSL2. For most people, especially those
    of you that are relatively new to programming, WSL1 is the best option. WSL2 is still a bit rough around the edges,
    and you can always upgrade later. WSL2 also doesn't play very nicely with Type 2 Hypervisors such as VirtualBox and vmware, since it uses Hyper-V - this will restrict you to using Hyper-V for any virtual machines that you decide to make. If you're someone that uses Docker or really
    needs native Linux GUIs, <a href="#wsl2">click here for instructions on how to install WSL2.</a>
</p>

<h3>Installing WSL1</h3>
<div class="alert">
    You need to be on at least Windows 10 version 1803 to use WSL. You can check your Windows version by opening
    Search and entering <code>winver</code>.
    If you need to update Windows, go
    <a target="_blank" href="https://support.microsoft.com/en-au/help/3159635">here</a>.
</div>

<ol>
    <li>
        <strong>Open PowerShell.</strong> Type in PowerShell into Windows Search, right click on the result and
        click <strong>Run as administrator</strong>. Select Yes if a prompt shows up.
    </li>
    <li>
        <strong>Run the following command:</strong>
        <pre>Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Windows-Subsystem-Linux</pre>
        If you are prompted to restart your computer, do so.
    </li>
    <li>
        <strong>Open the Microsoft Store.</strong> You can again do this using Windows Search.
    </li>
    <li>
        <strong>Search for Ubuntu and install it.</strong> Ubuntu currently has the best support for WSL.
    </li>
    <li>
        <strong>Once installed, open Ubuntu.</strong> You can do this by opening Search and typing in
        <code>bash</code>. Once you do this, a terminal should open!
    </li>
    <li>
        <strong>You'll be guided through setting up an account.</strong> Make sure you remember what password
        you use.
    </li>
    <li>
        <strong>We can now install a C compiler.</strong> We'll be using gcc. You can use it just like dcc, but
        you'll get less helpful error messages. To install, run the following command:
        <pre>sudo apt-get install build-essential</pre>
        Enter your password when prompted.
    </li>
    <li>
        <strong>Run the following command to make sure everything is up to date.</strong>
        <pre>sudo apt update && sudo apt upgrade</pre>
    </li>
</ol>
<p>
    You should now be able to use the text based Linux commands you learn during the course, replacing dcc with
    gcc. (Note: If you've learnt about <code>dcc --leak-check</code>, it won't work with gcc.)<br>
    If you use VSCode, you can use <code>code [filename]</code> to open a file. If you use Vim, Vim will work as
    normal. However, gedit won't work. Google 'wsl x server' if you're really desperate to use it.
</p>

<p>
    If you want a prettier terminal with more features to use with WSL, <a href="#wt">click here</a>
    to jump to those instructions. If you want to install dcc, <a href="#dcc">click here</a>.
</p>


<h3 id="wsl2">Installing WSL2</h3>
<div class="alert">
    You need to be on at least Windows 10 version 1903 to use WSL2. If you aren't, you should probably use WSL1.
</div>
<ol>
    <li>
        <strong>Follow Step 1 and 2 from the above section.</strong>
    </li>
    <li>
        <strong>Run</strong> <code>dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart</code>
        in the same PowerShell window.
    </li>
    <li>
        <strong>Download and run</strong> <a href="https://aka.ms/wsl2kernel" target="_blank">this installer from Microsoft.</a>
    </li>
    <li>
        <strong>Run </strong> <code>wsl --set-default-version 2</code> in PowerShell.
    </li>
    
    <li>
        <strong>Follow Step 3 onwards from the above section.</strong>
    </li>
</ol>


<h4>Why is WSL2 seemingly eating my RAM?</h4>
<p>
    If you have WSL2 running for a while, you might see a process called Vmmem in Task Manager eating up all your RAM. You can
    be relieved to know that this isn't the result of malware, but WSL2 using memory for cache. However, this isn't an issue
    whatsoever - if another process on your computer needs more RAM, WSL2 will reduce its cache usage - in fact, Windows itself
    uses any remaining unused RAM as cache to speed up your computer, because unused RAM is wasted RAM.
</p>
<p>
    If this RAM usage still somewhat irrationally annoys you, you can do one of the following:
    <ul>
        <li>
            <strong>Run the following command in your WSL2 terminal.</strong> 
            <code>sudo sh -c \"echo 3 >'/proc/sys/vm/drop_caches' && swapoff -a && swapon -a && printf '\n%s\n' 'Cached RAM and Swap Cleared'\"</code>
        </li>
        <li>
            Go to <code>%userprofile%</code> in File Explorer (by entering it into the address bar) and creating a file called .wslconfig and entering
            the following:
<pre>
[wsl2]
memory=4096MB
</pre>
            This restricts WSL2 to 4GB of RAM. If you're running Docker containers from WSL2, I encourage you to let WSL2 use at least half your system
            memory - I have mine at 12GB, or 60%. If you don't know what Docker is, you can just ignore this.
        </li>
    </ul>
</p>

<h2 id="dcc">Installing dcc</h2>

<p>Your mileage may vary, but you can install dcc locally! Run the following commands:</p>
<ol>
    <li><code>curl -L https://github.com/COMP1511UNSW/dcc/releases/download/2.6.4/dcc_2.6.4_all.deb -o
            /tmp/dcc_2.6.4_all.deb</code><br>curl is
        a utility in Linux to download files from the internet. You may also see wget sometimes, which is very
        similar.
    </li>
    <li>
        <code>sudo apt install /tmp/dcc_2.6.4_all.deb</code><br>This installs the package we just downloaded.
    </li>
</ol>

<p>
    Now, try compiling and running a program. If this doesn't work, run the command<br>
    <code>
    sudo bash -c "echo 0 > /proc/sys/kernel/yama/ptrace_scope;echo 1 > /proc/sys/vm/overcommit_memory"
    </code>,
    but only do this <strong>if you really need to.</strong>
</p>
<p>
    Note that you should still follow the steps to install build-essential from earlier because it has a lot
    of other useful tools.
</p>

<h2 id="wt">Very optional but very cool: Installing Windows Terminal</h2>
<p>This part is very very optional, but installs <a target="_blank" rel="noopener noreferrer"
        href="https://www.youtube.com/watch?v=8gw0rXPMMPE">Windows Terminal</a>, which allows you to access cmd,
    powershell and bash in one place, open multiple terminals (even side-by-side), and most importantly, use themes!</p>
<img style="max-width:100%" class="post-image" src="/assets/images/blog/cse-setup-old/wt.jpg" alt="Windows Terminal" />
<div class="alert">
    You need to be on at least Windows 10 version 1909 for this to work. You can check your Windows version by opening
    Search and entering <code>winver</code>.
    If you're need to update Windows, go <a target="_blank" rel="noopener noreferrer"
        href="https://support.microsoft.com/en-au/help/3159635/windows-10-update-assistant">here</a>.
</div>
<ol>
    <li><strong>Open the Microsoft Store.</strong></li>
    <li><strong>Search for Windows Terminal and install it.</strong> Once installed, you can open it by entering
        <code>wt</code> or searching Windows Terminal in Search.</li>
    <li><strong>If you want, you can adjust settings.</strong> If you know how to read a JSON file, use
        <kbd>Ctrl+,</kbd> to access settings. You can even find themes <a target="_blank" rel="noopener noreferrer"
            href="https://terminalsplash.com">here</a>.</li>
</ol>
<h2 id="git">Setting up Git and GitHub</h2>
<p>
    Git is a really useful tool which allows you backup your code, revert to older versions, keep track of changes,
    update and pull your code from multiple devices,
    and provides several tools to allow you to work collaboratively. This is called version control.
</p>
<h3>Signing up for GitHub</h3>
<p><em>Every</em> developer has a GitHub account. GitHub provides servers for you to host your code, using Git.
</p>
<ol>
    <li><strong>Sign up for GitHub <a target="_blank" rel="noopener noreferrer"
                href="https://github.com/join">here</a>.</strong>
        This shouldn't take more than a minute</li>
    <li><strong>Get the GitHub Education Pack!</strong> GitHub Education is really handy. Students
        can get GitHub Pro, as well as heaps of other useful tools for free</strong>.
        Visit <a href="https://education.github.com/pack" target="_blank"
            rel="noopener noreferrer">education.github.com</a> to sign up!</li>
    <li><strong>Follow someone on GitHub for the first time!</strong> You can score bonus points by <abbr
            title="Obviously me creating this post was just a ploy to get GitHub followers :P">following me</abbr>
        <a href="https://github.com/abiramen/" target="_blank" rel="noopener noreferrer">here</a> :)</li>
    <li><strong>Create your first repository <a href="https://github.com/new" target="_blank"
                rel="noopener noreferrer">here</a>.</strong> Think of a
        repository (or repo for short) as somewhere you store all your code for a project. Perhaps call your
        repo hello-world.</li>
</ol>
<p>Note: If you're ever using GitHub for coursework, make sure that you make your repo private to prevent
    plagiarism.</p>
<h3>Installing Git</h3>
<p>If you're working on VNC, or with the CSE machines over SSH, Git is already installed on these machines!
    Otherwise, you'll need to install Git.</p>
<h5>For Ubuntu/Debian (including WSL):</h5>
<p>
    Run <code>sudo apt-get install git</code> from your terminal.
</p>
<h5>For Windows and macOS:</h5>
<p>Download Git from <a target="_blank" rel="noopener noreferrer" href="https://git-scm.com/downloads">here</a>, and
    follow the
    instructions to install.</p>
<h4>Setting up Git on each machine</h4>
<ol>
    <li><strong>Telling Git your name. </strong>Run <code>git config --global user.name "yourusername"</code> -
        I
        usually just use my GitHub username here.</li>
    <li><strong>Telling Git your email. </strong>Run <code>git config --global user.email
            "name@example.com"</code> - I usually use the private GitHub email ending with
        @users.noreply.github.com,
        which can be found <a target="_blank" rel="noopener noreferrer"
            href="https://github.com/settings/emails">here</a> under 'Keep email addresses
        private'.</li>
</ol>
<h3>Making use of our new repo</h3>
<p>Now, we want to make use of our repo, and push code to it.</p>
<ol>
    <li><strong>Create a folder </strong> called HelloWorld somewhere on your computer.</li>
    <li><strong>Open a terminal</strong> and use <code>cd</code> until you're in your new folder.</li>
    <li><strong>Initialise a <em>local</em> repository</strong> using the command <code>git init</code>. This
        means you've set up a repository within your own computer, which tracks changes.</li>
    <li>
        <strong>Make a hello_world.c file in the folder.</strong>
        Here's some code for you to steal.
        <pre>
#include &lt;stdio.h&gt;
int main(void) {
    printf("Hello world!");
    return 0;
}
</pre>
    </li>
    <li><strong>Run <code>git add hello_world.c</code>.</strong> This tells git that you want changes to the
        file to be
        included in the next update. If you ever have multiple files that you want to update, use
        <code>git add --all</code>.</li>
    <li><strong>Run <code>git commit -m "Initialise repo"</code>.</strong> This keeps track of the update,
        noting what changes you've made, and stores it on the <em>local copy of the repository</em>. Your commit
        message should always describe the changes you've made (in present tense!).</li>
    <li><strong>Now, we want to sync our local repository with our remote repository on GitHub.</strong> The
        first time we do this, we have to tell git where our remote repository is. We do this with
        <code>git remote add origin https://github.com/your-username/your-repo-name.git</code></li>
    <li><strong>We can finally push the change to our remote repository.</strong> Use <code>git push</code>.</li>
</ol>
<p>You should now see your hello_world.c file at https://github.com/your-username/your-repo-name!<br> You should
    always commit and push your code whenever you've
    made a notable change, so that you can revert your code if you ever have problems later on. Note that
    git will
    ask for your GitHub password each time you push. We can avoid this by <a href="#ssh-git"
        class="page-anchor-offset">setting up an SSH
        keypair</a>, which you can scroll down to read about.</p>
<h3>Using git to work across multiple computers</h3>
<p>We can make a copy of our hello-world repository on another computer using
    <code>git clone https://github.com/your-username/your-repo-name.git</code>. This sets up a new local
    repository with the contents
    from the remote repository. Now, we can work on our code on this new computer, and use git add, commit and
    push
    to update our GitHub repository just like before. Now, when we return to our
    original computer, we run
    <code>git pull</code> to fetch updates from the remote repository. as soon as we start. This is <strong>very
        important</strong> to do
    - if you don't, and realise too late, you'll have to deal with resolving what's called a merge conflict.</p>
<p>
    There is much, much more to Git. If you want to learn more about Git, and you're a UNSW student, take COMP1531,
    which teaches you all about Git, especially in the context of teamwork. You can also learn from the slides for this
    <a href="http://bit.ly/GitGud2020Slides"></a>CSESoc workshop.</a>
</p> <img style="max-width:100%" class="post-image" src="/assets/images/blog/cse-setup-old/git.jpg" alt="Git" />

<h2 id="ssh-git">Using SSH keypairs to push to GitHub</h2>
<h3>Part 1: Generating a keypair</h3>
<p>
    You'll first need to generate a keypair. If you haven't done this, check out the guide in my
    <a href="/cse-setup#ssh-vsc" target="_blank">other post.</a> If you have, you can use the same
    keypair.
</p>

<h3>Part 2: Adding your public key to GitHub</h3>
<ol>
    <li><strong>Open the GitHub Key Settings page. </strong> <a target="_blank"
            href="https://github.com/settings/ssh/">You can find it here.</a> Press Add SSH key.</li>
    <li><strong>Give the key a name to describe what computer it is from.</strong> This is so that you don't get
        mixed up if you add more computers.</li>
    <li><strong>Paste the public key you copied earlier.</strong> Press Add when you are done.</li>
</ol>
<img class="post-image" style="max-width:100%" src="/assets/images/blog/cse-setup-old/ssh-github.jpg" alt="GitHub" />
<h3>Part 3: Updating your local repositories</h3>
<p>If you already have any local repositories, you need to update them to use the SSH Git path. To do this,
    <code>cd</code> to the directory, and enter
    <code>git remote set-url origin git@github.com:your-username/your-repo-name</code>. <br>If you clone or set
    up any new repositories, you will also have
    to use the SSH address, i.e. <code>git clone git@github.com:your-username/your-repo-name</code>. If you're
    ever unsure as to the SSH path, you can check
    on GitHub.
</p>
<img class="post-image" style="max-width:100%" src="/assets/images/blog/cse-setup-old/ssh-repo.jpg"
    alt="SSH key for a repo" />]]></content><author><name></name></author><category term="unsw-cse" /><category term="guide" /><category term="win-terminal" /><category term="wsl" /><category term="git" /><summary type="html"><![CDATA[This post is no longer up to date. I generally now advise most people use WSL 2. I may or may not write an updated version of this post in the future.]]></summary></entry></feed>